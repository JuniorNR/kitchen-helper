# 📦 Entities

Документация бизнес-сущностей приложения Kitchen Helper.

## 📂 Структура

Entities содержат бизнес-логику и модели данных для основных сущностей приложения.

```
entities/
├── 💬 chat/                # Сущность чата
├── 🥘 ingredient/          # Сущность ингредиента
├── 🛒 market/              # Сущность магазина
├── 🍳 recipe/              # Сущность рецепта
└── 👤 user/                # Сущность пользователя
```

## 🎯 Принципы

Каждая entity содержит:
- **model/** - бизнес-логика, API запросы, типы, хуки
- **index.ts** - публичный API модуля

## 📚 Сущности

### [[FrontEnd/Kitchen-helper/docs/🎨 Features/💬 Chat|📦 Entities/💬 Chat]]

Сущность чата для обмена сообщениями между пользователями.

**Компоненты**:
- `chat.api.ts` - RTK Query API для работы с чатами
- `chat.types.ts` - TypeScript типы для чата
- `useChat.ts` - хук для работы с чатом

**Связанные документы**:
- [[💬 Chat Feature|🎨 Features/💬 Chat]]

---

### [[🥘 Ingredient|📦 Entities/🥘 Ingredient]]

Сущность ингредиента для рецептов.

**Компоненты**:
- `ingredient.api.ts` - RTK Query API для работы с ингредиентами
- `ingredient.types.ts` - TypeScript типы для ингредиентов
- `useIngredient.ts` - хук для работы с ингредиентами

**Связанные документы**:
- [[🥘 IngredientsList Feature|🎨 Features/🥘 IngredientsList]]
- [[➕ IngredientCreate Feature|🎨 Features/➕ IngredientCreate]]

---

### [[🛒 Market|📦 Entities/🛒 Market]]

Сущность магазина для покупок ингредиентов.

**Компоненты**:
- `market.api.ts` - RTK Query API для работы с магазинами
- `market.types.ts` - TypeScript типы для магазинов
- `market.utils.ts` - утилиты для работы с магазинами
- `useMarket.ts` - хук для работы с магазинами

**Связанные документы**:
- [[🛒 MarketList Feature|🎨 Features/🛒 MarketList]]

---

### [[🍳 Recipe|📦 Entities/🍳 Recipe]]

Сущность рецепта блюда.

**Компоненты**:
- `recipe.api.ts` - RTK Query API для работы с рецептами
- `recipe.type.ts` - TypeScript типы для рецептов
- `recipe.utils.ts` - утилиты для работы с рецептами
- `useRecipe.ts` - хук для работы с рецептами

**Связанные документы**:
- [[🍳 RecipesList Feature|🎨 Features/🍳 RecipesList]]
- [[➕ RecipeCreate Feature|🎨 Features/➕ RecipeCreate]]

---

### [[👤 User|📦 Entities/👤 User]]

Сущность пользователя приложения.

**Компоненты**:
- `user.api.ts` - RTK Query API для работы с пользователями
- `user.types.ts` - TypeScript типы для пользователей
- `useUser.ts` - хук для работы с пользователями

**Связанные документы**:
- [[🔐 Auth Feature|🎨 Features/🔐 Auth]]
- [[⚙️ UserSettings Feature|🎨 Features/⚙️ UserSettings]]

---

## 🔄 Паттерны работы

### RTK Query API

Все entities используют RTK Query для работы с API:

```typescript
export const entityApi = createApi({
  reducerPath: 'entityApi',
  baseQuery: baseQuery,
  tagTypes: ['Entity'],
  endpoints: (builder) => ({
    // endpoints
  }),
});
```

### DTO преобразование

Данные преобразуются между форматами сервера (snake_case) и клиента (camelCase) с помощью функции `dto`:

```typescript
import { dto } from '@/shared/lib/helpers';

transformResponse: (response: EntityDTO[]) => {
  return dto<EntityDTO[], Entity[]>('toClient', response);
}
```

### Кастомные хуки

Каждая entity экспортирует хук для удобной работы:

```typescript
export const useEntity = (params) => {
  // RTK Query хуки
  // Бизнес-логика
  // Возврат данных и функций
};
```

## 🔗 Связанные документы

- [[📁 Структура проекта|📁 Структура проекта]]
- [[🎨 Features|🎨 Features]]
- [[🔧 Shared/lib/helpers/dto|DTO Helper]]
- [[🔧 Shared/lib/store|Redux Store]]

