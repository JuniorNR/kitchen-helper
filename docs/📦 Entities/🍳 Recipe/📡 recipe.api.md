# 📡 recipe.api.ts

RTK Query API для работы с рецептами.

## 📍 Расположение

`src/entities/recipe/model/recipe.api.ts`

## 📝 Описание

Определяет endpoints для работы с рецептами через RTK Query.

## 🔧 Endpoints

### `getRecipes`

Получение списка рецептов с пагинацией и фильтрацией.

- **Метод**: GET
- **URL**: `/recipes`
- **Параметры**: `{ page, filters }`
- **Возвращает**: `ApiResponse<Recipe[]>`
- **Tags**: `['Recipe']`

**Особенности**:
- Поддержка фильтрации через `RecipeListFilter`
- Автоматическое преобразование DTO
- Обработка ошибок с показом уведомлений

---

### `createRecipe`

Создание нового рецепта.

- **Метод**: POST
- **URL**: `/recipes/create`
- **Тело**: `FormData` (через `buildRecipeCreateFormData`)
- **Возвращает**: `{ recipe: Recipe, code: string }`
- **Tags**: Инвалидирует `['Recipe']`

**Особенности**:
- Использует FormData для загрузки изображений
- Показывает уведомления об успехе/ошибке

---

### `deleteRecipe`

Удаление рецепта.

- **Метод**: DELETE
- **URL**: `/recipes/delete/:id`
- **Параметры**: `id` (string)
- **Возвращает**: `{ code: string }`
- **Tags**: Инвалидирует `['Recipe']`

---

## 🔧 Экспорты

- `recipeApi` - RTK Query API instance
- `useGetRecipesQuery` - хук для получения рецептов
- `useCreateRecipeMutation` - хук для создания
- `useDeleteRecipeMutation` - хук для удаления

---

## 🔗 Связанные документы

- [[🍳 Recipe|📦 Entities/🍳 Recipe]]
- [[🍳 Recipe/🛠️ recipe.utils|recipe.utils.ts]]
- [[🍳 Recipe/🪝 useRecipe|useRecipe.ts]]

