# 📦 Инкапсуляция

**Инкапсуляция** — это механизм объединения данных и методов, работающих с этими данными, в единую структуру (класс), при этом скрывая внутреннюю реализацию от внешнего использования.

## 🎯 Суть концепции

Инкапсуляция позволяет:
- ✅ Скрывать внутреннюю реализацию объекта
- ✅ Защищать данные от неправильного использования
- ✅ Контролировать доступ к данным через методы
- ✅ Упрощать использование объекта (пользователю не нужно знать детали)

## 🔒 Модификаторы доступа

В большинстве языков программирования существуют модификаторы доступа:

### Public (Публичный)
Доступен везде, где виден класс.

```typescript
class BankAccount {
  public balance: number; // Доступен извне
  
  public getBalance(): number {
    return this.balance;
  }
}
```

### Private (Приватный)
Доступен только внутри класса.

```typescript
class BankAccount {
  private accountNumber: string; // Недоступен извне
  
  private validateAccount(): boolean {
    // Внутренняя логика валидации
    return true;
  }
}
```

### Protected (Защищенный)
Доступен внутри класса и его наследников.

```typescript
class BankAccount {
  protected accountType: string; // Доступен в наследниках
}
```

## 💡 Пример: Банковский счет

### ❌ Плохой пример (без инкапсуляции)

```typescript
class BankAccount {
  public balance: number = 0;
}

const account = new BankAccount();
account.balance = -1000; // Можно установить отрицательный баланс!
account.balance = 999999; // Можно установить любое значение
```

**Проблемы:**
- Нет контроля над данными
- Можно установить недопустимые значения
- Нет валидации

### ✅ Хороший пример (с инкапсуляцией)

```typescript
class BankAccount {
  private balance: number = 0;
  private accountNumber: string;

  constructor(accountNumber: string, initialBalance: number = 0) {
    this.accountNumber = accountNumber;
    if (initialBalance >= 0) {
      this.balance = initialBalance;
    }
  }

  // Публичные методы для работы с балансом
  public deposit(amount: number): boolean {
    if (amount > 0) {
      this.balance += amount;
      return true;
    }
    return false;
  }

  public withdraw(amount: number): boolean {
    if (amount > 0 && amount <= this.balance) {
      this.balance -= amount;
      return true;
    }
    return false;
  }

  public getBalance(): number {
    return this.balance;
  }

  // Приватный метод для внутренней логики
  private logTransaction(type: string, amount: number): void {
    console.log(`${type}: ${amount} on account ${this.accountNumber}`);
  }
}

const account = new BankAccount("12345", 1000);
account.deposit(500);  // ✅ Работает
account.withdraw(200); // ✅ Работает
// account.balance = -1000; // ❌ Ошибка! balance приватный
console.log(account.getBalance()); // 1300
```

## 🎯 Преимущества инкапсуляции

### 1. Защита данных
Данные защищены от неправильного использования.

### 2. Контроль доступа
Можно контролировать, как и когда данные изменяются.

### 3. Гибкость реализации
Можно изменить внутреннюю реализацию, не затрагивая внешний код.

### 4. Упрощение использования
Пользователю класса не нужно знать детали реализации.

## 🔄 Связь с другими концепциями

### Инкапсуляция и Абстракция
Инкапсуляция помогает реализовать абстракцию, скрывая ненужные детали.

### Инкапсуляция и Наследование
Приватные поля не наследуются, защищенные — наследуются.

### Инкапсуляция и SOLID
Инкапсуляция поддерживает принцип [[🔌 Single Responsibility|🔌 Single Responsibility]] и [[🔓 Open-Closed|🔓 Open-Closed]].

## 📝 Практические советы

1. **Делайте поля приватными по умолчанию**
   - Открывайте доступ только при необходимости

2. **Используйте геттеры и сеттеры**
   - Для контроля доступа к данным

3. **Скрывайте сложную логику**
   - Внутренние методы должны быть приватными

4. **Предоставляйте простой публичный интерфейс**
   - Пользователю не нужно знать, как все работает внутри

## 🔗 Связанные документы

- [[🧬 Наследование|🧬 Наследование]] - Как инкапсуляция работает с наследованием
- [[📝 Абстракция|📝 Абстракция]] - Связь инкапсуляции и абстракции
- [[🔌 Single Responsibility|🔌 Single Responsibility]] - SOLID принцип, связанный с инкапсуляцией
- [[🎓 ООП|🎓 ООП]] - Обзор ООП концепций

---

*Инкапсуляция — это не просто скрытие данных, это контроль и защита.*

