# 🏗️ Классы и объекты

**Класс** — это шаблон или чертеж для создания объектов. **Объект** — это конкретный экземпляр класса с реальными значениями.

## 🎯 Основные понятия

### Класс (Class)
- Шаблон или план для создания объектов
- Определяет структуру данных (поля/свойства)
- Определяет поведение (методы)
- Не занимает память до создания объекта

### Объект (Object / Instance)
- Конкретный экземпляр класса
- Имеет реальные значения в полях
- Может выполнять действия через методы
- Занимает память

## 💡 Простая аналогия

**Класс** = Чертеж дома
- Определяет, сколько комнат, этажей, окон
- Но сам по себе не является домом

**Объект** = Конкретный дом
- Построен по чертежу
- Имеет конкретные размеры, адрес, цвет

## 📝 Базовый пример

```typescript
// КЛАСС - это шаблон
class Person {
  // Поля (свойства) - данные объекта
  private name: string;
  private age: number;

  // Конструктор - метод для создания объекта
  constructor(name: string, age: number) {
    this.name = name;
    this.age = age;
  }

  // Методы - действия, которые может выполнять объект
  public introduce(): void {
    console.log(`Hi, I'm ${this.name} and I'm ${this.age} years old`);
  }

  public getAge(): number {
    return this.age;
  }

  public haveBirthday(): void {
    this.age++;
  }
}

// ОБЪЕКТЫ - конкретные экземпляры класса
const person1 = new Person("Alice", 25);
const person2 = new Person("Bob", 30);
const person3 = new Person("Charlie", 35);

// Каждый объект независим
person1.introduce(); // "Hi, I'm Alice and I'm 25 years old"
person2.introduce(); // "Hi, I'm Bob and I'm 30 years old"

person1.haveBirthday();
console.log(person1.getAge()); // 26
console.log(person2.getAge()); // 30 (не изменился!)
```

## 🏗️ Структура класса

### 1. Поля (Fields / Properties)
Хранят данные объекта.

```typescript
class Car {
  // Публичное поле
  public brand: string;
  
  // Приватное поле
  private mileage: number = 0;
  
  // Защищенное поле
  protected engineType: string;
  
  // Статическое поле (принадлежит классу, а не объекту)
  static totalCars: number = 0;
}
```

### 2. Конструктор (Constructor)
Метод, вызываемый при создании объекта.

```typescript
class Car {
  private brand: string;
  private model: string;

  constructor(brand: string, model: string) {
    this.brand = brand;
    this.model = model;
    Car.totalCars++; // Увеличиваем счетчик
  }
}
```

### 3. Методы (Methods)
Действия, которые может выполнять объект.

```typescript
class Car {
  private speed: number = 0;

  // Публичный метод
  public accelerate(): void {
    this.speed += 10;
  }

  // Приватный метод
  private checkEngine(): boolean {
    return true;
  }

  // Статический метод
  static getTotalCars(): number {
    return Car.totalCars;
  }
}
```

## 🔄 Создание объектов

### Базовое создание
```typescript
const car = new Car("Toyota", "Camry");
```

### С параметрами
```typescript
class Rectangle {
  private width: number;
  private height: number;

  constructor(width: number, height: number) {
    this.width = width;
    this.height = height;
  }
}

const rect = new Rectangle(10, 5);
```

### С значениями по умолчанию
```typescript
class Rectangle {
  private width: number;
  private height: number;

  constructor(width: number = 1, height: number = 1) {
    this.width = width;
    this.height = height;
  }
}

const rect1 = new Rectangle();      // width=1, height=1
const rect2 = new Rectangle(10);    // width=10, height=1
const rect3 = new Rectangle(10, 5); // width=10, height=5
```

## 🎯 Пример: Банковский счет

```typescript
class BankAccount {
  private accountNumber: string;
  private balance: number;
  private owner: string;

  constructor(accountNumber: string, owner: string, initialBalance: number = 0) {
    this.accountNumber = accountNumber;
    this.owner = owner;
    this.balance = initialBalance;
  }

  public deposit(amount: number): boolean {
    if (amount > 0) {
      this.balance += amount;
      return true;
    }
    return false;
  }

  public withdraw(amount: number): boolean {
    if (amount > 0 && amount <= this.balance) {
      this.balance -= amount;
      return true;
    }
    return false;
  }

  public getBalance(): number {
    return this.balance;
  }

  public getInfo(): string {
    return `Account ${this.accountNumber} (${this.owner}): $${this.balance}`;
  }
}

// Создание объектов
const account1 = new BankAccount("12345", "Alice", 1000);
const account2 = new BankAccount("67890", "Bob", 500);

// Работа с объектами
account1.deposit(200);
account2.withdraw(100);

console.log(account1.getInfo()); // "Account 12345 (Alice): $1200"
console.log(account2.getInfo()); // "Account 67890 (Bob): $400"
```

## 🔑 Статические члены

Статические члены принадлежат классу, а не объекту.

```typescript
class MathHelper {
  // Статическое поле
  static PI: number = 3.14159;
  
  // Статический метод
  static add(a: number, b: number): number {
    return a + b;
  }
  
  static multiply(a: number, b: number): number {
    return a * b;
  }
}

// Использование без создания объекта
console.log(MathHelper.PI);              // 3.14159
console.log(MathHelper.add(5, 3));       // 8
console.log(MathHelper.multiply(4, 7)); // 28
```

## 🎭 Геттеры и сеттеры

```typescript
class Person {
  private _age: number = 0;

  // Геттер
  get age(): number {
    return this._age;
  }

  // Сеттер с валидацией
  set age(value: number) {
    if (value >= 0 && value <= 150) {
      this._age = value;
    } else {
      throw new Error("Invalid age");
    }
  }
}

const person = new Person();
person.age = 25;        // Использует сеттер
console.log(person.age); // 25 (использует геттер)
```

## 🔗 Связь с другими концепциями

### Классы и Инкапсуляция
Классы позволяют инкапсулировать данные и методы вместе.

### Классы и Наследование
Классы могут наследоваться от других классов.

### Классы и Полиморфизм
Объекты разных классов могут использоваться полиморфно.

### Классы и Абстракция
Абстрактные классы определяют общий интерфейс.

## 📝 Практические советы

1. **Используйте классы для моделирования сущностей**
   - Класс должен представлять одну концепцию

2. **Делайте поля приватными по умолчанию**
   - Используйте геттеры/сеттеры для доступа

3. **Инициализируйте поля в конструкторе**
   - Гарантируйте валидное состояние объекта

4. **Используйте статические методы для утилит**
   - Методы, не требующие состояния объекта

5. **Следуйте принципу единственной ответственности**
   - Класс должен делать одну вещь хорошо

## 🔗 Связанные документы

- [[📦 Инкапсуляция|📦 Инкапсуляция]] - Как скрывать детали в классах
- [[🧬 Наследование|🧬 Наследование]] - Создание иерархий классов
- [[🔀 Полиморфизм|🔀 Полиморфизм]] - Работа с объектами разных классов
- [[📝 Абстракция|📝 Абстракция]] - Абстрактные классы
- [[🔌 Single Responsibility|🔌 Single Responsibility]] - Принцип проектирования классов
- [[🎓 ООП|🎓 ООП]] - Обзор ООП концепций

---

*Класс — это план, объект — это реальность.*

