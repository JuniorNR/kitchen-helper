# 🔗 Связь между концепциями ООП

Все четыре основных принципа ООП тесно связаны и работают вместе для создания качественного объектно-ориентированного кода.

## 🎯 Как концепции работают вместе

### 1. Абстракция → Инкапсуляция → Наследование → Полиморфизм

```
Абстракция определяет ЧТО нужно
    ↓
Инкапсуляция скрывает КАК это реализовано
    ↓
Наследование переиспользует и расширяет
    ↓
Полиморфизм использует единый интерфейс
```

## 💡 Комплексный пример

Рассмотрим систему управления транспортными средствами, где все концепции работают вместе:

```typescript
// 1. АБСТРАКЦИЯ - определяем, что должно быть
abstract class Vehicle {
  // 2. ИНКАПСУЛЯЦИЯ - скрываем детали реализации
  protected brand: string;
  protected speed: number = 0;
  private engineStatus: boolean = false;

  constructor(brand: string) {
    this.brand = brand;
  }

  // Абстрактные методы - каждый класс реализует по-своему
  abstract startEngine(): void;
  abstract accelerate(): void;

  // Общие методы на уровне абстракции
  public stop(): void {
    this.speed = 0;
    this.engineStatus = false;
  }

  public getSpeed(): number {
    return this.speed;
  }

  // Инкапсуляция - скрываем внутреннее состояние
  protected setEngineStatus(status: boolean): void {
    this.engineStatus = status;
  }

  protected isEngineRunning(): boolean {
    return this.engineStatus;
  }
}

// 3. НАСЛЕДОВАНИЕ - расширяем базовый класс
class Car extends Vehicle {
  private doors: number;

  constructor(brand: string, doors: number) {
    super(brand);
    this.doors = doors;
  }

  // Реализация абстрактных методов
  public startEngine(): void {
    this.setEngineStatus(true);
    console.log(`${this.brand} car engine started`);
  }

  public accelerate(): void {
    if (this.isEngineRunning()) {
      this.speed += 20;
    }
  }

  // Специфичный метод для Car
  public openDoors(): void {
    console.log(`Opening ${this.doors} doors`);
  }
}

class Bicycle extends Vehicle {
  private gears: number;

  constructor(brand: string, gears: number) {
    super(brand);
    this.gears = gears;
  }

  // Другая реализация абстрактных методов
  public startEngine(): void {
    // У велосипеда нет двигателя, но интерфейс тот же
    console.log(`${this.brand} bicycle ready to ride`);
  }

  public accelerate(): void {
    this.speed += 5; // Велосипед ускоряется медленнее
  }

  // Специфичный метод для Bicycle
  public changeGear(gear: number): void {
    if (gear > 0 && gear <= this.gears) {
      console.log(`Changed to gear ${gear}`);
    }
  }
}

// 4. ПОЛИМОРФИЗМ - используем единый интерфейс
function testVehicle(vehicle: Vehicle): void {
  vehicle.startEngine();
  vehicle.accelerate();
  vehicle.accelerate();
  console.log(`Speed: ${vehicle.getSpeed()} km/h`);
  vehicle.stop();
}

// Создаем объекты разных типов
const car = new Car("Toyota", 4);
const bicycle = new Bicycle("Trek", 21);

// Полиморфное использование
testVehicle(car);      // Работает с Car
testVehicle(bicycle);  // Работает с Bicycle

// Массив разных типов
const vehicles: Vehicle[] = [car, bicycle];

// Полиморфная обработка
vehicles.forEach(vehicle => {
  vehicle.startEngine();
  vehicle.accelerate();
  console.log(vehicle.getSpeed());
});
```

## 🔄 Взаимодействие концепций

### Абстракция + Инкапсуляция
- **Абстракция** определяет, что показывать пользователю
- **Инкапсуляция** скрывает, как это реализовано

```typescript
// Абстракция: показываем только нужный интерфейс
interface PaymentProcessor {
  processPayment(amount: number): boolean;
}

// Инкапсуляция: скрываем детали реализации
class CreditCardProcessor implements PaymentProcessor {
  private validateCard(cardNumber: string): boolean {
    // Сложная логика валидации скрыта
    return true;
  }

  private connectToBank(): void {
    // Детали подключения скрыты
  }

  public processPayment(amount: number): boolean {
    // Пользователь видит только этот метод
    if (!this.validateCard("...")) return false;
    this.connectToBank();
    // Обработка платежа
    return true;
  }
}
```

### Наследование + Полиморфизм
- **Наследование** создает иерархию классов
- **Полиморфизм** использует эту иерархию единообразно

```typescript
// Наследование создает иерархию
abstract class Shape {
  abstract calculateArea(): number;
}

class Circle extends Shape {
  calculateArea(): number {
    return Math.PI * this.radius * this.radius;
  }
}

class Rectangle extends Shape {
  calculateArea(): number {
    return this.width * this.height;
  }
}

// Полиморфизм использует иерархию
function printArea(shape: Shape): void {
  console.log(shape.calculateArea()); // Работает с любым Shape
}

const shapes: Shape[] = [new Circle(5), new Rectangle(4, 6)];
shapes.forEach(printArea);
```

### Абстракция + Наследование + Полиморфизм
- **Абстракция** определяет интерфейс
- **Наследование** реализует интерфейс
- **Полиморфизм** использует разные реализации

```typescript
// Абстракция
abstract class Notification {
  abstract send(): void;
}

// Наследование - разные реализации
class EmailNotification extends Notification {
  send(): void {
    console.log("Sending email");
  }
}

class SMSNotification extends Notification {
  send(): void {
    console.log("Sending SMS");
  }
}

// Полиморфизм - единый интерфейс
function sendNotifications(notifications: Notification[]): void {
  notifications.forEach(n => n.send());
}
```

## 🎯 Практические паттерны

### Паттерн Template Method
Сочетает абстракцию, наследование и полиморфизм:

```typescript
// Абстракция - шаблонный метод
abstract class DataProcessor {
  // Шаблонный метод - определяет алгоритм
  public process(): void {
    this.loadData();
    this.transformData();
    this.saveData();
  }

  // Абстрактные шаги - каждый класс реализует по-своему
  protected abstract loadData(): void;
  protected abstract transformData(): void;
  protected abstract saveData(): void;
}

// Наследование - конкретные реализации
class CSVProcessor extends DataProcessor {
  protected loadData(): void {
    console.log("Loading CSV data");
  }

  protected transformData(): void {
    console.log("Transforming CSV data");
  }

  protected saveData(): void {
    console.log("Saving CSV data");
  }
}

class JSONProcessor extends DataProcessor {
  protected loadData(): void {
    console.log("Loading JSON data");
  }

  protected transformData(): void {
    console.log("Transforming JSON data");
  }

  protected saveData(): void {
    console.log("Saving JSON data");
  }
}

// Полиморфизм - используем единый интерфейс
const processors: DataProcessor[] = [
  new CSVProcessor(),
  new JSONProcessor()
];

processors.forEach(p => p.process());
```

## 📊 Сводная таблица

| Концепция | Что делает | Как связана с другими |
|-----------|------------|----------------------|
| **Абстракция** | Упрощает, выделяет важное | Определяет интерфейс для инкапсуляции и полиморфизма |
| **Инкапсуляция** | Скрывает детали | Реализует абстракцию, защищает данные для наследования |
| **Наследование** | Переиспользует код | Использует абстракцию, создает основу для полиморфизма |
| **Полиморфизм** | Единый интерфейс | Использует абстракцию и наследование |

## 🔗 Связанные документы

- [[📦 Инкапсуляция|📦 Инкапсуляция]] - Сокрытие деталей
- [[🧬 Наследование|🧬 Наследование]] - Переиспользование кода
- [[🔀 Полиморфизм|🔀 Полиморфизм]] - Единый интерфейс
- [[📝 Абстракция|📝 Абстракция]] - Упрощение систем
- [[🏗️ Классы и объекты|🏗️ Классы и объекты]] - Основы ООП
- [[🎓 ООП|🎓 ООП]] - Обзор ООП концепций

---

*ООП концепции не существуют изолированно — они работают вместе для создания качественного кода.*

