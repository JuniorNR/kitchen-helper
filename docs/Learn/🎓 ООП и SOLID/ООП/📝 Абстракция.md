# 📝 Абстракция

**Абстракция** — это процесс упрощения сложных систем путем выделения только существенных характеристик и скрытия ненужных деталей реализации.

## 🎯 Суть концепции

Абстракция позволяет:
- ✅ Упрощать сложные системы
- ✅ Фокусироваться на важных аспектах
- ✅ Скрывать детали реализации
- ✅ Создавать понятные интерфейсы

## 📐 Уровни абстракции

### Высокий уровень абстракции
Общие концепции, без деталей реализации.

```typescript
interface PaymentProcessor {
  processPayment(amount: number): boolean;
}
```

### Низкий уровень абстракции
Конкретные детали реализации.

```typescript
class CreditCardProcessor implements PaymentProcessor {
  private validateCard(cardNumber: string): boolean {
    // Детальная логика валидации
    return cardNumber.length === 16;
  }

  public processPayment(amount: number): boolean {
    // Детальная логика обработки платежа
    return true;
  }
}
```

## 💡 Пример: Система управления транспортом

### Высокий уровень абстракции

```typescript
// Абстракция - что транспорт должен делать
abstract class Vehicle {
  abstract start(): void;
  abstract stop(): void;
  abstract getSpeed(): number;
  
  // Общая логика на уровне абстракции
  public isMoving(): boolean {
    return this.getSpeed() > 0;
  }
}
```

### Конкретные реализации

```typescript
// Конкретная реализация - как это работает для автомобиля
class Car extends Vehicle {
  private engineStarted: boolean = false;
  private speed: number = 0;

  public start(): void {
    this.engineStarted = true;
    console.log("Car engine started");
  }

  public stop(): void {
    this.engineStarted = false;
    this.speed = 0;
    console.log("Car stopped");
  }

  public getSpeed(): number {
    return this.speed;
  }

  public accelerate(): void {
    if (this.engineStarted) {
      this.speed += 10;
    }
  }
}

// Другая конкретная реализация
class Bicycle extends Vehicle {
  private speed: number = 0;

  public start(): void {
    console.log("Started pedaling");
  }

  public stop(): void {
    this.speed = 0;
    console.log("Stopped pedaling");
  }

  public getSpeed(): number {
    return this.speed;
  }

  public pedal(): void {
    this.speed += 5;
  }
}
```

## 🎭 Абстракция через интерфейсы

```typescript
// Абстракция - что должно быть, а не как
interface Database {
  connect(): void;
  disconnect(): void;
  query(sql: string): any[];
}

// Конкретные реализации скрывают детали
class MySQLDatabase implements Database {
  private connection: any;

  public connect(): void {
    // Детали подключения к MySQL
    console.log("Connecting to MySQL...");
  }

  public disconnect(): void {
    // Детали отключения от MySQL
    console.log("Disconnecting from MySQL...");
  }

  public query(sql: string): any[] {
    // Детали выполнения запроса в MySQL
    return [];
  }
}

class PostgreSQLDatabase implements Database {
  public connect(): void {
    // Детали подключения к PostgreSQL
    console.log("Connecting to PostgreSQL...");
  }

  public disconnect(): void {
    // Детали отключения от PostgreSQL
    console.log("Disconnecting from PostgreSQL...");
  }

  public query(sql: string): any[] {
    // Детали выполнения запроса в PostgreSQL
    return [];
  }
}

// Использование абстракции
function useDatabase(db: Database): void {
  db.connect();
  const results = db.query("SELECT * FROM users");
  db.disconnect();
  // Не важно, какая БД используется - интерфейс одинаковый
}
```

## 🔄 Абстрактные классы vs Интерфейсы

### Абстрактные классы
Могут содержать реализацию и абстрактные методы.

```typescript
abstract class Animal {
  // Конкретный метод
  public breathe(): void {
    console.log("Breathing...");
  }

  // Абстрактный метод - должен быть реализован
  abstract makeSound(): void;
}

class Dog extends Animal {
  public makeSound(): void {
    console.log("Woof!");
  }
}
```

### Интерфейсы
Определяют только контракт, без реализации.

```typescript
interface Flyable {
  fly(): void;
}

interface Swimmable {
  swim(): void;
}

class Duck implements Flyable, Swimmable {
  public fly(): void {
    console.log("Duck is flying");
  }

  public swim(): void {
    console.log("Duck is swimming");
  }
}
```

## 🎯 Преимущества абстракции

### 1. Упрощение
Сложные системы становятся понятнее.

### 2. Переиспользование
Абстракции можно использовать в разных контекстах.

### 3. Гибкость
Легко менять реализации, не затрагивая код, использующий абстракцию.

### 4. Тестируемость
Легко создавать моки и стабы для тестирования.

## 💡 Пример: Система уведомлений

```typescript
// Абстракция - что должно быть
abstract class Notification {
  protected recipient: string;
  protected message: string;

  constructor(recipient: string, message: string) {
    this.recipient = recipient;
    this.message = message;
  }

  // Абстрактный метод - каждая реализация делает по-своему
  abstract send(): void;

  // Общая логика на уровне абстракции
  protected validate(): boolean {
    return this.recipient.length > 0 && this.message.length > 0;
  }
}

// Конкретные реализации скрывают детали
class EmailNotification extends Notification {
  public send(): void {
    if (!this.validate()) return;
    // Детали отправки email скрыты
    console.log(`Sending email to ${this.recipient}: ${this.message}`);
  }
}

class SMSNotification extends Notification {
  public send(): void {
    if (!this.validate()) return;
    // Детали отправки SMS скрыты
    console.log(`Sending SMS to ${this.recipient}: ${this.message}`);
  }
}

// Использование абстракции
function sendNotification(notification: Notification): void {
  notification.send(); // Не важно, какой тип уведомления
}
```

## 🔗 Связь с другими концепциями

### Абстракция и Инкапсуляция
Абстракция определяет, что показывать, инкапсуляция — как скрывать детали.

### Абстракция и Наследование
Абстрактные классы часто используются как базовые для наследования.

### Абстракция и Полиморфизм
Абстракция определяет интерфейс для полиморфного поведения.

### Абстракция и SOLID
- [[🎯 Interface Segregation|🎯 Interface Segregation]] — разделение абстракций
- [[🏗️ Dependency Inversion|🏗️ Dependency Inversion]] — зависимость от абстракций
- [[🔓 Open-Closed|🔓 Open-Closed]] — расширение через абстракции

## 📝 Практические советы

1. **Создавайте абстракции на нужном уровне**
   - Не слишком общие, не слишком конкретные

2. **Используйте интерфейсы для контрактов**
   - Интерфейсы более гибкие, чем абстрактные классы

3. **Скрывайте детали реализации**
   - Показывайте только то, что нужно пользователю

4. **Избегайте утечки абстракций**
   - Не раскрывайте детали реализации через абстракцию

5. **Применяйте принцип наименьшего знания**
   - Объект должен знать только о необходимых абстракциях

## ⚠️ Антипаттерны

### ❌ Плохо: Утечка абстракции
```typescript
interface Database {
  connect(): void;
  // Утечка - раскрывает детали MySQL
  executeMySQLQuery(query: string): void;
}
```

### ✅ Хорошо: Правильная абстракция
```typescript
interface Database {
  connect(): void;
  query(sql: string): any[]; // Абстрактный метод
}
```

## 🔗 Связанные документы

- [[📦 Инкапсуляция|📦 Инкапсуляция]] - Как скрывать детали реализации
- [[🧬 Наследование|🧬 Наследование]] - Абстрактные классы как базовые
- [[🔀 Полиморфизм|🔀 Полиморфизм]] - Абстракция для полиморфного поведения
- [[🎯 Interface Segregation|🎯 Interface Segregation]] - Разделение абстракций
- [[🏗️ Dependency Inversion|🏗️ Dependency Inversion]] - Зависимость от абстракций
- [[🎓 ООП|🎓 ООП]] - Обзор ООП концепций

---

*Абстракция — это искусство видеть лес, а не деревья.*

