# 🔀 Полиморфизм

**Полиморфизм** — это способность объектов разных типов обрабатываться через единый интерфейс, при этом каждый тип может иметь свою собственную реализацию.

## 🎯 Суть концепции

Полиморфизм позволяет:
- ✅ Использовать один интерфейс для разных типов
- ✅ Вызывать методы без знания конкретного типа объекта
- ✅ Писать более гибкий и расширяемый код
- ✅ Упрощать работу с коллекциями разных типов

## 📐 Типы полиморфизма

### 1. Параметрический полиморфизм (Generics)
Один код работает с разными типами.

```typescript
function identity<T>(arg: T): T {
  return arg;
}

const num = identity<number>(42);
const str = identity<string>("hello");
```

### 2. Полиморфизм подтипов (Subtype Polymorphism)
Объекты разных классов обрабатываются через общий интерфейс.

```typescript
class Animal {
  public makeSound(): void {
    console.log("Some sound");
  }
}

class Dog extends Animal {
  public makeSound(): void {
    console.log("Woof!");
  }
}

class Cat extends Animal {
  public makeSound(): void {
    console.log("Meow!");
  }
}

// Полиморфизм в действии
const animals: Animal[] = [
  new Dog(),
  new Cat(),
  new Animal()
];

animals.forEach(animal => {
  animal.makeSound(); // Вызывает правильный метод для каждого типа
});
```

## 💡 Пример: Фигуры

```typescript
// Базовый класс
abstract class Shape {
  abstract calculateArea(): number;
  abstract calculatePerimeter(): number;

  public getInfo(): string {
    return `Area: ${this.calculateArea()}, Perimeter: ${this.calculatePerimeter()}`;
  }
}

// Класс-наследник
class Circle extends Shape {
  private radius: number;

  constructor(radius: number) {
    super();
    this.radius = radius;
  }

  public calculateArea(): number {
    return Math.PI * this.radius * this.radius;
  }

  public calculatePerimeter(): number {
    return 2 * Math.PI * this.radius;
  }
}

// Еще один класс-наследник
class Rectangle extends Shape {
  private width: number;
  private height: number;

  constructor(width: number, height: number) {
    super();
    this.width = width;
    this.height = height;
  }

  public calculateArea(): number {
    return this.width * this.height;
  }

  public calculatePerimeter(): number {
    return 2 * (this.width + this.height);
  }
}

// Полиморфизм в действии
const shapes: Shape[] = [
  new Circle(5),
  new Rectangle(4, 6),
  new Circle(3)
];

shapes.forEach(shape => {
  console.log(shape.getInfo());
  // Каждый объект использует свою реализацию calculateArea()
});
```

## 🎭 Полиморфизм через интерфейсы

```typescript
// Интерфейс определяет контракт
interface Drawable {
  draw(): void;
}

class Circle implements Drawable {
  public draw(): void {
    console.log("Drawing a circle");
  }
}

class Square implements Drawable {
  public draw(): void {
    console.log("Drawing a square");
  }
}

class Triangle implements Drawable {
  public draw(): void {
    console.log("Drawing a triangle");
  }
}

// Функция работает с любым объектом, реализующим Drawable
function renderShapes(shapes: Drawable[]): void {
  shapes.forEach(shape => {
    shape.draw(); // Полиморфный вызов
  });
}

const shapes: Drawable[] = [
  new Circle(),
  new Square(),
  new Triangle()
];

renderShapes(shapes);
```

## 🔄 Переопределение методов (Method Overriding)

```typescript
class Animal {
  public move(): void {
    console.log("Animal is moving");
  }
}

class Dog extends Animal {
  // Переопределение метода
  public move(): void {
    console.log("Dog is running");
  }
}

class Fish extends Animal {
  // Переопределение метода
  public move(): void {
    console.log("Fish is swimming");
  }
}

const dog = new Dog();
const fish = new Fish();

dog.move();  // "Dog is running"
fish.move(); // "Fish is swimming"

// Полиморфизм
const animals: Animal[] = [dog, fish];
animals.forEach(animal => animal.move());
```

## 🎯 Преимущества полиморфизма

### 1. Гибкость
Один код работает с разными типами объектов.

### 2. Расширяемость
Легко добавлять новые типы без изменения существующего кода.

### 3. Упрощение
Не нужно знать конкретный тип объекта для работы с ним.

### 4. Поддержка принципа Open-Closed
Код открыт для расширения, закрыт для модификации.

## 💡 Пример: Система уведомлений

```typescript
interface Notification {
  send(): void;
}

class EmailNotification implements Notification {
  public send(): void {
    console.log("Sending email notification");
  }
}

class SMSNotification implements Notification {
  public send(): void {
    console.log("Sending SMS notification");
  }
}

class PushNotification implements Notification {
  public send(): void {
    console.log("Sending push notification");
  }
}

// Полиморфная функция
function sendNotifications(notifications: Notification[]): void {
  notifications.forEach(notification => {
    notification.send(); // Каждый тип отправляется по-своему
  });
}

const notifications: Notification[] = [
  new EmailNotification(),
  new SMSNotification(),
  new PushNotification()
];

sendNotifications(notifications);
```

## 🔗 Связь с другими концепциями

### Полиморфизм и Наследование
Полиморфизм часто реализуется через наследование — дочерние классы переопределяют методы родителя.

### Полиморфизм и Абстракция
Абстрактные классы и интерфейсы определяют контракт для полиморфного поведения.

### Полиморфизм и SOLID
- [[🔓 Open-Closed|🔓 Open-Closed]] — полиморфизм позволяет расширять без изменения
- [[🔄 Liskov Substitution|🔄 Liskov Substitution]] — основа для правильного полиморфизма
- [[🎯 Interface Segregation|🎯 Interface Segregation]] — разделение интерфейсов для полиморфизма

## 📝 Практические советы

1. **Используйте интерфейсы для полиморфизма**
   - Интерфейсы более гибкие, чем наследование

2. **Следуйте принципу подстановки Лисков**
   - Объекты должны быть взаимозаменяемыми

3. **Избегайте проверок типов (type checking)**
   - Используйте полиморфизм вместо if/switch по типам

4. **Применяйте Strategy Pattern**
   - Полиморфизм — основа паттерна Strategy

5. **Используйте абстрактные классы для общей логики**
   - Абстрактные методы для полиморфного поведения

## ⚠️ Антипаттерны

### ❌ Плохо: Проверка типов
```typescript
function process(animal: Animal): void {
  if (animal instanceof Dog) {
    // Специфичная логика для Dog
  } else if (animal instanceof Cat) {
    // Специфичная логика для Cat
  }
}
```

### ✅ Хорошо: Полиморфизм
```typescript
abstract class Animal {
  abstract process(): void;
}

class Dog extends Animal {
  public process(): void {
    // Логика для Dog
  }
}

function process(animal: Animal): void {
  animal.process(); // Полиморфный вызов
}
```

## 🔗 Связанные документы

- [[🧬 Наследование|🧬 Наследование]] - Основа для полиморфизма подтипов
- [[📝 Абстракция|📝 Абстракция]] - Абстракция определяет интерфейс для полиморфизма
- [[🔄 Liskov Substitution|🔄 Liskov Substitution]] - Правила полиморфизма
- [[🔓 Open-Closed|🔓 Open-Closed]] - Как полиморфизм поддерживает открытость/закрытость
- [[🎓 ООП|🎓 ООП]] - Обзор ООП концепций

---

*Полиморфизм — это способность видеть общее в разном и разное в общем.*

