# 🧬 Наследование

**Наследование** — это механизм создания нового класса на основе существующего класса, позволяющий переиспользовать код и создавать иерархии классов.

## 🎯 Суть концепции

Наследование позволяет:
- ✅ Переиспользовать код из родительского класса
- ✅ Расширять функциональность без изменения существующего кода
- ✅ Создавать иерархии классов
- ✅ Применять принцип DRY (Don't Repeat Yourself)

## 📐 Основные термины

- **Базовый класс (родительский, суперкласс)** — класс, от которого наследуются другие
- **Производный класс (дочерний, подкласс)** — класс, который наследуется от базового
- **Переопределение методов** — замена реализации метода в дочернем классе

## 💡 Пример: Транспортные средства

```typescript
// Базовый класс
class Vehicle {
  protected brand: string;
  protected speed: number = 0;

  constructor(brand: string) {
    this.brand = brand;
  }

  public accelerate(): void {
    this.speed += 10;
  }

  public getSpeed(): number {
    return this.speed;
  }

  public getInfo(): string {
    return `${this.brand} - Speed: ${this.speed} km/h`;
  }
}

// Дочерний класс - наследует от Vehicle
class Car extends Vehicle {
  private doors: number;

  constructor(brand: string, doors: number) {
    super(brand); // Вызов конструктора родителя
    this.doors = doors;
  }

  // Переопределение метода
  public accelerate(): void {
    this.speed += 20; // Машина ускоряется быстрее
  }

  // Новый метод, специфичный для Car
  public openDoors(): void {
    console.log(`Opening ${this.doors} doors`);
  }
}

// Еще один дочерний класс
class Bicycle extends Vehicle {
  private gears: number;

  constructor(brand: string, gears: number) {
    super(brand);
    this.gears = gears;
  }

  // Переопределение метода
  public accelerate(): void {
    this.speed += 5; // Велосипед ускоряется медленнее
  }

  // Новый метод
  public changeGear(gear: number): void {
    if (gear > 0 && gear <= this.gears) {
      console.log(`Changed to gear ${gear}`);
    }
  }
}

// Использование
const car = new Car("Toyota", 4);
const bike = new Bicycle("Trek", 21);

car.accelerate();
bike.accelerate();

console.log(car.getInfo()); // "Toyota - Speed: 20 km/h"
console.log(bike.getInfo()); // "Trek - Speed: 5 km/h"
```

## 🔄 Многоуровневое наследование

```typescript
class Animal {
  protected name: string;

  constructor(name: string) {
    this.name = name;
  }

  public makeSound(): void {
    console.log("Some sound");
  }
}

class Dog extends Animal {
  public makeSound(): void {
    console.log("Woof!");
  }

  public fetch(): void {
    console.log(`${this.name} is fetching`);
  }
}

class Puppy extends Dog {
  public makeSound(): void {
    console.log("Yip yip!");
  }

  public play(): void {
    console.log(`${this.name} is playing`);
  }
}

const puppy = new Puppy("Max");
puppy.makeSound(); // "Yip yip!"
puppy.fetch();     // "Max is fetching" (унаследовано от Dog)
puppy.play();      // "Max is playing"
```

## 🎯 Преимущества наследования

### 1. Переиспользование кода
Не нужно дублировать код в каждом классе.

### 2. Расширяемость
Можно добавлять новую функциональность, не изменяя существующий код.

### 3. Иерархия
Создает логическую структуру классов.

### 4. Полиморфизм
Позволяет использовать дочерние классы вместо родительских.

## ⚠️ Ограничения и проблемы

### 1. Жесткая связанность
Дочерний класс тесно связан с родительским.

### 2. Нарушение инкапсуляции
Дочерний класс зависит от реализации родителя.

### 3. Проблема хрупкого базового класса
Изменения в базовом классе могут сломать дочерние классы.

### 4. Множественное наследование
В некоторых языках (например, TypeScript/JavaScript) нет множественного наследования классов.

## 🔀 Композиция vs Наследование

### Наследование (IS-A)
"Автомобиль **является** транспортным средством"

```typescript
class Car extends Vehicle {
  // Car IS-A Vehicle
}
```

### Композиция (HAS-A)
"Автомобиль **имеет** двигатель"

```typescript
class Engine {
  public start(): void {
    console.log("Engine started");
  }
}

class Car {
  private engine: Engine; // Car HAS-A Engine

  constructor() {
    this.engine = new Engine();
  }

  public start(): void {
    this.engine.start();
  }
}
```

**Правило:** Предпочитайте композицию наследованию, когда это возможно.

## 🔒 Модификаторы доступа и наследование

```typescript
class Parent {
  public publicField: string = "public";
  protected protectedField: string = "protected";
  private privateField: string = "private";

  public publicMethod(): void {}
  protected protectedMethod(): void {}
  private privateMethod(): void {}
}

class Child extends Parent {
  public test(): void {
    this.publicField;      // ✅ Доступно
    this.protectedField;   // ✅ Доступно
    // this.privateField;  // ❌ Недоступно

    this.publicMethod();    // ✅ Доступно
    this.protectedMethod(); // ✅ Доступно
    // this.privateMethod(); // ❌ Недоступно
  }
}
```

## 🔗 Связь с другими концепциями

### Наследование и Полиморфизм
Наследование позволяет использовать полиморфизм — обрабатывать объекты разных классов через единый интерфейс.

### Наследование и Абстракция
Абстрактные классы часто используются как базовые классы для наследования.

### Наследование и SOLID
- [[🔄 Liskov Substitution|🔄 Liskov Substitution]] — принцип подстановки Лисков для наследования
- [[🔓 Open-Closed|🔓 Open-Closed]] — наследование позволяет расширять без изменения

## 📝 Практические советы

1. **Используйте наследование для IS-A отношений**
   - Если класс "является" другим классом

2. **Избегайте глубоких иерархий**
   - Более 3-4 уровней наследования усложняют код

3. **Предпочитайте композицию наследованию**
   - Композиция более гибкая

4. **Не наследуйте для переиспользования кода**
   - Используйте композицию или миксины

5. **Следуйте принципу Лисков**
   - Дочерний класс должен быть заменяемым на родительский

## 🔗 Связанные документы

- [[🔀 Полиморфизм|🔀 Полиморфизм]] - Как наследование работает с полиморфизмом
- [[🔄 Liskov Substitution|🔄 Liskov Substitution]] - Принцип правильного наследования
- [[🔓 Open-Closed|🔓 Open-Closed]] - Как наследование поддерживает открытость/закрытость
- [[🎓 ООП|🎓 ООП]] - Обзор ООП концепций

---

*Наследование — это мощный инструмент, но используйте его с умом.*

