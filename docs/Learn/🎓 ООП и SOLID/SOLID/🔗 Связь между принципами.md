# 🔗 Связь между принципами SOLID

Все пять принципов SOLID тесно связаны и работают вместе для создания качественного кода. Понимание их взаимосвязи помогает применять их более эффективно.

## 🎯 Как принципы работают вместе

```
SRP (Единственная ответственность)
    ↓
Создает маленькие, сфокусированные классы
    ↓
OCP (Открытость/закрытость)
    ↓
Расширяет функциональность без изменения
    ↓
LSP (Подстановка Лисков)
    ↓
Гарантирует правильность расширений
    ↓
ISP (Разделение интерфейсов)
    ↓
Создает маленькие, специфичные интерфейсы
    ↓
DIP (Инверсия зависимостей)
    ↓
Связывает все через абстракции
```

## 💡 Комплексный пример

Рассмотрим систему обработки платежей, где все принципы SOLID работают вместе:

```typescript
// ========== SRP: Разделение ответственности ==========

// Класс только для данных платежа
class Payment {
  constructor(
    public amount: number,
    public currency: string
  ) {}
}

// Класс только для валидации
class PaymentValidator {
  public validate(payment: Payment): boolean {
    return payment.amount > 0 && payment.currency.length > 0;
  }
}

// ========== ISP: Разделение интерфейсов ==========

// Интерфейс только для обработки платежей
interface PaymentProcessor {
  process(payment: Payment): boolean;
}

// Интерфейс только для логирования
interface Logger {
  log(message: string): void;
}

// Интерфейс только для уведомлений
interface NotificationService {
  notify(message: string): void;
}

// ========== DIP: Инверсия зависимостей ==========

// Реализации для обработки платежей
class CreditCardProcessor implements PaymentProcessor {
  public process(payment: Payment): boolean {
    console.log(`Processing credit card payment: ${payment.amount}`);
    return true;
  }
}

class PayPalProcessor implements PaymentProcessor {
  public process(payment: Payment): boolean {
    console.log(`Processing PayPal payment: ${payment.amount}`);
    return true;
}

// Реализации для логирования
class ConsoleLogger implements Logger {
  public log(message: string): void {
    console.log(`[LOG] ${message}`);
  }
}

class FileLogger implements Logger {
  public log(message: string): void {
    console.log(`[FILE] ${message}`);
  }
}

// Реализации для уведомлений
class EmailNotificationService implements NotificationService {
  public notify(message: string): void {
    console.log(`[EMAIL] ${message}`);
  }
}

// ========== OCP: Открытость для расширения ==========

// Можно добавить новый процессор БЕЗ изменения существующего кода
class CryptocurrencyProcessor implements PaymentProcessor {
  public process(payment: Payment): boolean {
    console.log(`Processing crypto payment: ${payment.amount}`);
    return true;
  }
}

// ========== LSP: Правильное наследование ==========

// Базовый класс для всех процессоров (если нужна общая логика)
abstract class BasePaymentProcessor implements PaymentProcessor {
  protected abstract doProcess(payment: Payment): boolean;

  // Общая логика
  public process(payment: Payment): boolean {
    if (payment.amount <= 0) {
      return false;
    }
    return this.doProcess(payment);
  }
}

// Дочерние классы полностью заменяемы
class BankTransferProcessor extends BasePaymentProcessor {
  protected doProcess(payment: Payment): boolean {
    console.log(`Processing bank transfer: ${payment.amount}`);
    return true;
  }
}

// ========== Интеграция всех принципов ==========

class PaymentService {
  constructor(
    private processor: PaymentProcessor,  // DIP: зависимость от абстракции
    private validator: PaymentValidator,   // SRP: отдельная ответственность
    private logger: Logger,                 // DIP + ISP: маленький интерфейс
    private notification: NotificationService // DIP + ISP: маленький интерфейс
  ) {}

  public executePayment(payment: Payment): boolean {
    // SRP: каждая часть делает свою работу
    if (!this.validator.validate(payment)) {
      this.logger.log("Payment validation failed");
      return false;
    }

    const result = this.processor.process(payment);
    
    if (result) {
      this.logger.log(`Payment processed: ${payment.amount}`);
      this.notification.notify(`Payment of ${payment.amount} processed`);
    }

    return result;
  }
}

// Использование
const validator = new PaymentValidator();
const processor = new CreditCardProcessor();
const logger = new ConsoleLogger();
const notification = new EmailNotificationService();

const paymentService = new PaymentService(
  processor,
  validator,
  logger,
  notification
);

const payment = new Payment(100, "USD");
paymentService.executePayment(payment);
```

## 🔄 Взаимодействие принципов

### SRP ↔ OCP
- **SRP** создает маленькие классы с одной ответственностью
- **OCP** позволяет расширять эти классы без изменения

```typescript
// SRP: Класс с одной ответственностью
class EmailSender {
  public send(to: string, message: string): void {
    // Отправка email
  }
}

// OCP: Можно расширить через наследование
class HTMLEmailSender extends EmailSender {
  public send(to: string, message: string): void {
    // Отправка HTML email
  }
}
```

### OCP ↔ LSP
- **OCP** позволяет расширять через наследование
- **LSP** гарантирует, что расширения работают правильно

```typescript
// OCP: Базовый класс открыт для расширения
abstract class Shape {
  abstract getArea(): number;
}

// LSP: Дочерние классы полностью заменяемы
class Circle extends Shape {
  getArea(): number {
    return Math.PI * this.radius * this.radius;
  }
}
```

### ISP ↔ DIP
- **ISP** создает маленькие интерфейсы
- **DIP** использует эти интерфейсы для инверсии зависимостей

```typescript
// ISP: Маленький интерфейс
interface Logger {
  log(message: string): void;
}

// DIP: Зависимость от интерфейса
class Service {
  constructor(private logger: Logger) { }
}
```

### SRP ↔ ISP
- **SRP** разделяет ответственность классов
- **ISP** разделяет интерфейсы по ответственности

```typescript
// SRP: Разные классы для разных ответственностей
class UserValidator { }
class UserRepository { }
class EmailService { }

// ISP: Разные интерфейсы для разных ответственностей
interface Validator { }
interface Repository { }
interface EmailSender { }
```

## 📊 Матрица связей

| Принцип | Связан с | Как связаны |
|---------|----------|-------------|
| **SRP** | OCP | Маленькие классы легче расширять |
| **SRP** | ISP | Одна ответственность = один интерфейс |
| **OCP** | LSP | Расширения должны быть заменяемыми |
| **OCP** | DIP | Расширение через абстракции |
| **LSP** | OCP | Гарантирует правильность расширений |
| **ISP** | DIP | Маленькие интерфейсы для инверсии |
| **DIP** | Все | Связывает все через абстракции |

## 🎯 Практические паттерны

### Паттерн Strategy + SOLID
```typescript
// ISP: Разделенный интерфейс
interface SortStrategy {
  sort(data: number[]): number[];
}

// SRP: Каждый класс делает одну вещь
class QuickSort implements SortStrategy {
  sort(data: number[]): number[] {
    // Quick sort
    return data;
  }
}

class MergeSort implements SortStrategy {
  sort(data: number[]): number[] {
    // Merge sort
    return data;
  }
}

// OCP: Можно добавить новый алгоритм без изменения
class HeapSort implements SortStrategy {
  sort(data: number[]): number[] {
    // Heap sort
    return data;
  }
}

// DIP: Зависимость от абстракции
class Sorter {
  constructor(private strategy: SortStrategy) { }
  
  public sort(data: number[]): number[] {
    return this.strategy.sort(data);
  }
}
```

## 🔗 Связанные документы

- [[🔌 Single Responsibility|🔌 Single Responsibility]] - Единственная ответственность
- [[🔓 Open-Closed|🔓 Open-Closed]] - Открытость/закрытость
- [[🔄 Liskov Substitution|🔄 Liskov Substitution]] - Подстановка Лисков
- [[🎯 Interface Segregation|🎯 Interface Segregation]] - Разделение интерфейсов
- [[🏗️ Dependency Inversion|🏗️ Dependency Inversion]] - Инверсия зависимостей
- [[🎯 SOLID|🎯 SOLID]] - Обзор всех принципов

---

*SOLID принципы работают вместе, создавая гибкий и поддерживаемый код.*

