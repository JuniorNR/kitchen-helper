# 🔄 Liskov Substitution Principle (LSP)

**Принцип подстановки Лисков** — объекты подклассов должны заменять объекты базовых классов без нарушения работы программы.

## 🎯 Суть принципа

> "Объекты в программе должны заменяться экземплярами их подтипов без изменения правильности программы"

Дочерний класс должен быть полностью заменяемым на родительский класс.

## 📐 Формальное определение

Если S является подтипом T, то объекты типа T могут быть заменены объектами типа S без изменения желательных свойств программы.

## ❌ Проблема: Нарушение LSP

```typescript
// ❌ ПЛОХО: Нарушение принципа подстановки
class Rectangle {
  protected width: number;
  protected height: number;

  public setWidth(width: number): void {
    this.width = width;
  }

  public setHeight(height: number): void {
    this.height = height;
  }

  public getArea(): number {
    return this.width * this.height;
  }
}

// Квадрат наследуется от прямоугольника
class Square extends Rectangle {
  // Нарушение LSP: Square изменяет поведение базового класса
  public setWidth(width: number): void {
    this.width = width;
    this.height = width; // Квадрат всегда имеет равные стороны
  }

  public setHeight(height: number): void {
    this.width = height;
    this.height = height; // Квадрат всегда имеет равные стороны
  }
}

// Функция ожидает Rectangle
function testRectangle(rect: Rectangle): void {
  rect.setWidth(5);
  rect.setHeight(4);
  // Ожидаем площадь = 20
  console.log(rect.getArea()); // Для Square будет 16, а не 20!
}
```

**Проблемы:**
- Square не может заменить Rectangle
- Изменяется ожидаемое поведение
- Нарушение контракта базового класса

## ✅ Решение: Применение LSP

```typescript
// ✅ ХОРОШО: Правильная иерархия

// Базовый класс для всех фигур
abstract class Shape {
  abstract getArea(): number;
}

// Прямоугольник - конкретная реализация
class Rectangle extends Shape {
  constructor(
    protected width: number,
    protected height: number
  ) {
    super();
  }

  public setWidth(width: number): void {
    this.width = width;
  }

  public setHeight(height: number): void {
    this.height = height;
  }

  public getArea(): number {
    return this.width * this.height;
  }
}

// Квадрат - конкретная реализация
class Square extends Shape {
  constructor(private side: number) {
    super();
  }

  public setSide(side: number): void {
    this.side = side;
  }

  public getArea(): number {
    return this.side * this.side;
  }
}

// Функция работает с абстракцией Shape
function calculateTotalArea(shapes: Shape[]): number {
  return shapes.reduce((total, shape) => {
    return total + shape.getArea(); // Работает с любым Shape
  }, 0);
}

// Оба класса могут использоваться взаимозаменяемо
const shapes: Shape[] = [
  new Rectangle(5, 4),
  new Square(5)
];

console.log(calculateTotalArea(shapes)); // Работает корректно
```

## 💡 Пример: Птицы

### ❌ Без LSP

```typescript
class Bird {
  public fly(): void {
    console.log("Flying");
  }

  public eat(): void {
    console.log("Eating");
  }
}

class Sparrow extends Bird {
  // Sparrow может летать - OK
}

class Penguin extends Bird {
  // ❌ Проблема: Пингвин не может летать!
  public fly(): void {
    throw new Error("Penguins can't fly!");
  }
}

// Функция ожидает, что все птицы могут летать
function makeBirdFly(bird: Bird): void {
  bird.fly(); // Для Penguin выбросит ошибку!
}
```

### ✅ С LSP

```typescript
// Базовый класс для всех птиц
class Bird {
  public eat(): void {
    console.log("Eating");
  }
}

// Интерфейс для летающих птиц
interface Flyable {
  fly(): void;
}

// Летающие птицы
class Sparrow extends Bird implements Flyable {
  public fly(): void {
    console.log("Flying");
  }
}

class Eagle extends Bird implements Flyable {
  public fly(): void {
    console.log("Flying high");
  }
}

// Нелетающие птицы
class Penguin extends Bird {
  public swim(): void {
    console.log("Swimming");
  }
}

// Функция работает только с летающими птицами
function makeBirdFly(bird: Flyable): void {
  bird.fly(); // Гарантированно работает
}
```

## 🎯 Правила LSP

### 1. Предусловия не могут быть усилены
Дочерний класс не может требовать больше, чем базовый.

```typescript
// Базовый класс
class Base {
  public process(value: number): void {
    if (value > 0) { // Предусловие: value > 0
      // Обработка
    }
  }
}

// ❌ Плохо: Усиление предусловия
class Derived extends Base {
  public process(value: number): void {
    if (value > 10) { // ❌ Требует больше, чем базовый класс
      // Обработка
    }
  }
}
```

### 2. Постусловия не могут быть ослаблены
Дочерний класс должен гарантировать не меньше, чем базовый.

```typescript
// Базовый класс
class Base {
  public process(): number {
    const result = this.calculate();
    if (result < 0) {
      throw new Error("Result must be positive");
    }
    return result; // Постусловие: результат >= 0
  }
}

// ❌ Плохо: Ослабление постусловия
class Derived extends Base {
  public process(): number {
    const result = this.calculate();
    // ❌ Не проверяет, что результат >= 0
    return result;
  }
}
```

### 3. Инварианты должны сохраняться
Дочерний класс должен сохранять инварианты базового класса.

```typescript
class BankAccount {
  protected balance: number = 0; // Инвариант: balance >= 0

  public withdraw(amount: number): void {
    if (this.balance >= amount) {
      this.balance -= amount;
    }
  }
}

// ❌ Плохо: Нарушение инварианта
class OverdraftAccount extends BankAccount {
  public withdraw(amount: number): void {
    this.balance -= amount; // ❌ Может сделать balance < 0
  }
}
```

## 🔍 Как проверить LSP?

### Тест подстановки
Если заменить объект базового класса на объект дочернего класса, программа должна работать корректно.

```typescript
// Если этот код работает с Base
function useBase(base: Base): void {
  base.doSomething();
}

// То этот код должен работать с Derived
function useBase(derived: Derived): void {
  derived.doSomething(); // Должно работать так же
}
```

## 📝 Практические советы

1. **Следуйте контракту базового класса**
   - Дочерний класс должен выполнять все обещания базового

2. **Не изменяйте ожидаемое поведение**
   - Переопределяйте методы, но сохраняйте семантику

3. **Используйте интерфейсы для разных поведений**
   - Не все птицы летают, не все животные плавают

4. **Избегайте наследования для ограничения**
   - Если дочерний класс не может делать то же, что родитель

5. **Применяйте композицию вместо наследования**
   - Когда наследование нарушает LSP

## ⚠️ Частые нарушения LSP

1. **Бросание исключений в переопределенных методах**
   ```typescript
   class Base {
     public method(): void { }
   }
   
   class Derived extends Base {
     public method(): void {
       throw new Error("Not supported"); // ❌ Нарушение LSP
     }
   }
   ```

2. **Возврат null вместо объекта**
   ```typescript
   class Base {
     public getItem(): Item {
       return new Item();
     }
   }
   
   class Derived extends Base {
     public getItem(): Item {
       return null; // ❌ Нарушение LSP
     }
   }
   ```

3. **Изменение типа возвращаемого значения**
   ```typescript
   class Base {
     public process(): number {
       return 0;
     }
   }
   
   class Derived extends Base {
     public process(): string { // ❌ Нарушение LSP
       return "0";
     }
   }
   ```

## 🔗 Связь с другими принципами

### LSP и Наследование
LSP определяет правила правильного наследования.

### LSP и Полиморфизм
LSP гарантирует, что полиморфизм работает корректно.

### LSP и OCP
LSP обеспечивает, что расширения (OCP) работают правильно.

## 🔗 Связанные документы

- [[🧬 Наследование|🧬 Наследование]] - Правила наследования
- [[🔀 Полиморфизм|🔀 Полиморфизм]] - Основа для LSP
- [[🔓 Open-Closed|🔓 Open-Closed]] - LSP обеспечивает правильное расширение
- [[🎯 SOLID|🎯 SOLID]] - Обзор всех принципов

---

*Дочерний класс должен быть полностью заменяемым на родительский.*

