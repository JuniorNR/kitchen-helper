# 🎯 SOLID принципы

**SOLID** — это набор из пяти принципов объектно-ориентированного проектирования, которые помогают создавать гибкий, поддерживаемый и расширяемый код.

## 📋 Что означает SOLID

- **S** - [[🔌 Single Responsibility|Single Responsibility Principle]] (Принцип единственной ответственности)
- **O** - [[🔓 Open-Closed|Open-Closed Principle]] (Принцип открытости/закрытости)
- **L** - [[🔄 Liskov Substitution|Liskov Substitution Principle]] (Принцип подстановки Лисков)
- **I** - [[🎯 Interface Segregation|Interface Segregation Principle]] (Принцип разделения интерфейсов)
- **D** - [[🏗️ Dependency Inversion|Dependency Inversion Principle]] (Принцип инверсии зависимостей)

## 🎯 Зачем нужны SOLID принципы?

SOLID принципы помогают:
- ✅ Создавать код, который легко понимать
- ✅ Упрощать тестирование
- ✅ Уменьшать связанность между компонентами
- ✅ Повышать переиспользуемость кода
- ✅ Облегчать расширение функциональности
- ✅ Снижать риск появления багов

## 📐 Обзор принципов

### 🔌 Single Responsibility Principle (SRP)
Класс должен иметь только одну причину для изменения.

**Суть:** Один класс = одна ответственность.

### 🔓 Open-Closed Principle (OCP)
Программные сущности должны быть открыты для расширения, но закрыты для модификации.

**Суть:** Расширяйте функциональность через наследование/композицию, не изменяя существующий код.

### 🔄 Liskov Substitution Principle (LSP)
Объекты подклассов должны заменять объекты базовых классов без нарушения работы программы.

**Суть:** Дочерние классы должны быть полностью заменяемыми на родительские.

### 🎯 Interface Segregation Principle (ISP)
Клиенты не должны зависеть от интерфейсов, которые они не используют.

**Суть:** Создавайте маленькие, специфичные интерфейсы вместо больших общих.

### 🏗️ Dependency Inversion Principle (DIP)
Зависимости должны быть направлены на абстракции, а не на конкретные реализации.

**Суть:** Зависьте от интерфейсов, а не от конкретных классов.

## 🔗 Связь между принципами

Все принципы SOLID тесно связаны и работают вместе:

1. **SRP** создает маленькие, сфокусированные классы
2. **OCP** позволяет расширять эти классы без изменения
3. **LSP** гарантирует правильность наследования
4. **ISP** разделяет интерфейсы на специфичные части
5. **DIP** связывает все через абстракции

Подробнее: [[🔗 Связь между принципами|🔗 Связь между принципами]]

## 💡 Пример нарушения всех принципов

```typescript
// ❌ ПЛОХО: Нарушает все принципы SOLID
class User {
  // Нарушение SRP: класс делает слишком много
  public name: string;
  public email: string;

  // Нарушение SRP: работа с БД в классе пользователя
  public saveToDatabase(): void {
    // Сохранение в БД
  }

  // Нарушение SRP: отправка email в классе пользователя
  public sendEmail(): void {
    // Отправка email
  }

  // Нарушение SRP: валидация в классе пользователя
  public validate(): boolean {
    // Валидация
    return true;
  }
}
```

## ✅ Пример с применением SOLID

```typescript
// ✅ ХОРОШО: Применяем SOLID принципы

// SRP: Отдельный класс для пользователя
class User {
  constructor(
    public name: string,
    public email: string
  ) {}
}

// SRP: Отдельный класс для валидации
class UserValidator {
  public validate(user: User): boolean {
    return user.email.includes("@");
  }
}

// SRP: Отдельный класс для работы с БД
interface UserRepository {
  save(user: User): void;
}

class DatabaseUserRepository implements UserRepository {
  public save(user: User): void {
    // Сохранение в БД
  }
}

// SRP: Отдельный класс для отправки email
interface EmailService {
  send(to: string, message: string): void;
}

class SMTPEmailService implements EmailService {
  public send(to: string, message: string): void {
    // Отправка email
  }
}

// DIP: Зависим от абстракций
class UserService {
  constructor(
    private repository: UserRepository,
    private emailService: EmailService,
    private validator: UserValidator
  ) {}

  public registerUser(user: User): void {
    if (this.validator.validate(user)) {
      this.repository.save(user);
      this.emailService.send(user.email, "Welcome!");
    }
  }
}
```

## 📚 Порядок изучения

Рекомендуемый порядок изучения принципов:

1. **[[🔌 Single Responsibility|Single Responsibility]]** - Начните с самого простого
2. **[[🔓 Open-Closed|Open-Closed]]** - Учитесь расширять без изменения
3. **[[🔄 Liskov Substitution|Liskov Substitution]]** - Правильное наследование
4. **[[🎯 Interface Segregation|Interface Segregation]]** - Разделение интерфейсов
5. **[[🏗️ Dependency Inversion|Dependency Inversion]]** - Зависимость от абстракций

## 🎯 Практические советы

1. **Применяйте принципы постепенно**
   - Не пытайтесь применить все сразу
   - Начните с SRP, затем переходите к другим

2. **Не переусердствуйте**
   - SOLID — это инструменты, а не догма
   - Иногда простое решение лучше сложного

3. **Рефакторите постепенно**
   - Не нужно переписывать весь код сразу
   - Улучшайте по мере необходимости

4. **Используйте вместе с паттернами проектирования**
   - SOLID и паттерны дополняют друг друга

5. **Пишите тесты**
   - SOLID код легче тестировать
   - Тесты помогают проверить правильность применения принципов

## 🔗 Связанные документы

- [[🔌 Single Responsibility|🔌 Single Responsibility]] - Принцип единственной ответственности
- [[🔓 Open-Closed|🔓 Open-Closed]] - Принцип открытости/закрытости
- [[🔄 Liskov Substitution|🔄 Liskov Substitution]] - Принцип подстановки Лисков
- [[🎯 Interface Segregation|🎯 Interface Segregation]] - Принцип разделения интерфейсов
- [[🏗️ Dependency Inversion|🏗️ Dependency Inversion]] - Принцип инверсии зависимостей
- [[🔗 Связь между принципами|🔗 Связь между принципами]] - Как принципы работают вместе
- [[🎓 ООП|🎓 ООП]] - Основы ООП, на которых базируются SOLID

---

*SOLID принципы — это не правила, а руководящие принципы для создания качественного кода.*

