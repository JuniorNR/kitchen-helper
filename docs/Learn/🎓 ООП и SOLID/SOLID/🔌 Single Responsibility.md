# 🔌 Single Responsibility Principle (SRP)

**Принцип единственной ответственности** — класс должен иметь только одну причину для изменения.

## 🎯 Суть принципа

> "У класса должна быть только одна причина для изменения"

Класс должен отвечать только за одну вещь и делать её хорошо.

## ❌ Проблема: Нарушение SRP

```typescript
// ❌ ПЛОХО: Класс делает слишком много
class User {
  public name: string;
  public email: string;

  // Ответственность 1: Валидация
  public validate(): boolean {
    return this.email.includes("@");
  }

  // Ответственность 2: Работа с базой данных
  public saveToDatabase(): void {
    console.log(`Saving ${this.name} to database`);
  }

  // Ответственность 3: Отправка email
  public sendEmail(subject: string, body: string): void {
    console.log(`Sending email to ${this.email}`);
  }

  // Ответственность 4: Логирование
  public logActivity(action: string): void {
    console.log(`User ${this.name} performed: ${action}`);
  }
}
```

**Проблемы:**
- Класс имеет множество причин для изменения
- Сложно тестировать
- Нарушение принципа единственной ответственности
- Высокая связанность

## ✅ Решение: Применение SRP

```typescript
// ✅ ХОРОШО: Каждый класс отвечает за одну вещь

// Класс только для данных пользователя
class User {
  constructor(
    public name: string,
    public email: string
  ) {}
}

// Отдельный класс для валидации
class UserValidator {
  public validate(user: User): boolean {
    if (!user.email || !user.email.includes("@")) {
      return false;
    }
    if (!user.name || user.name.length < 2) {
      return false;
    }
    return true;
  }
}

// Отдельный класс для работы с базой данных
class UserRepository {
  public save(user: User): void {
    console.log(`Saving ${user.name} to database`);
    // Логика сохранения в БД
  }

  public findById(id: string): User | null {
    // Логика поиска в БД
    return null;
  }
}

// Отдельный класс для отправки email
class EmailService {
  public send(to: string, subject: string, body: string): void {
    console.log(`Sending email to ${to}: ${subject}`);
    // Логика отправки email
  }
}

// Отдельный класс для логирования
class Logger {
  public log(message: string): void {
    console.log(`[LOG] ${new Date().toISOString()}: ${message}`);
  }
}

// Сервис, координирующий работу других классов
class UserService {
  constructor(
    private validator: UserValidator,
    private repository: UserRepository,
    private emailService: EmailService,
    private logger: Logger
  ) {}

  public registerUser(user: User): void {
    if (this.validator.validate(user)) {
      this.repository.save(user);
      this.emailService.send(
        user.email,
        "Welcome!",
        "Thank you for registration"
      );
      this.logger.log(`User ${user.name} registered`);
    }
  }
}
```

## 🎯 Преимущества SRP

### 1. Легче понимать
Каждый класс делает одну вещь, код проще читать.

### 2. Легче тестировать
Можно тестировать каждую ответственность отдельно.

### 3. Легче изменять
Изменения в одной области не затрагивают другие.

### 4. Меньше связанности
Классы слабо связаны между собой.

### 5. Легче переиспользовать
Классы можно использовать в разных контекстах.

## 💡 Пример: Система отчетов

### ❌ Без SRP

```typescript
class Report {
  public generateReport(data: any[]): string {
    // Генерация отчета
    const report = this.formatData(data);
    
    // Сохранение в файл
    this.saveToFile(report);
    
    // Отправка по email
    this.sendEmail(report);
    
    return report;
  }

  private formatData(data: any[]): string {
    // Форматирование данных
    return "Formatted report";
  }

  private saveToFile(report: string): void {
    // Сохранение в файл
    console.log("Saving to file");
  }

  private sendEmail(report: string): void {
    // Отправка email
    console.log("Sending email");
  }
}
```

### ✅ С SRP

```typescript
// Класс только для генерации отчета
class ReportGenerator {
  public generate(data: any[]): string {
    return this.formatData(data);
  }

  private formatData(data: any[]): string {
    // Форматирование данных
    return "Formatted report";
  }
}

// Класс только для работы с файлами
class FileService {
  public save(content: string, filename: string): void {
    console.log(`Saving to file: ${filename}`);
    // Логика сохранения
  }
}

// Класс только для отправки email
class EmailService {
  public send(to: string, subject: string, body: string): void {
    console.log(`Sending email to ${to}`);
    // Логика отправки
  }
}

// Координатор
class ReportService {
  constructor(
    private generator: ReportGenerator,
    private fileService: FileService,
    private emailService: EmailService
  ) {}

  public createAndDistributeReport(data: any[]): void {
    const report = this.generator.generate(data);
    this.fileService.save(report, "report.txt");
    this.emailService.send("admin@example.com", "Report", report);
  }
}
```

## 🔍 Как определить нарушение SRP?

### Признаки нарушения:

1. **Класс делает слишком много разных вещей**
   ```typescript
   class User {
     validate() { }
     save() { }
     sendEmail() { }
     log() { }
   }
   ```

2. **Много методов, которые не связаны между собой**
   ```typescript
   class Utils {
     formatDate() { }
     calculateTax() { }
     parseJSON() { }
   }
   ```

3. **Сложно дать классу одно простое название**
   - Если название класса содержит "и" или "или" — возможно нарушение

4. **Изменения в одной части затрагивают другие**
   - Если изменение логики валидации требует изменения логики сохранения

## 📝 Практические советы

1. **Задавайте вопрос: "Что делает этот класс?"**
   - Если ответ содержит "и" — возможно нарушение SRP

2. **Один класс = одна ответственность**
   - Не смешивайте разные области ответственности

3. **Используйте композицию**
   - Объединяйте маленькие классы в большие сервисы

4. **Разделяйте данные и поведение**
   - Классы данных (DTO) отдельно от классов логики

5. **Следуйте правилу "одна причина для изменения"**
   - Если класс меняется по разным причинам — разделите его

## 🔗 Связь с другими принципами

### SRP и Open-Closed
SRP создает маленькие классы, которые легче расширять без изменения (OCP).

### SRP и Dependency Inversion
Маленькие классы с одной ответственностью легче заменять через абстракции (DIP).

### SRP и Interface Segregation
Одна ответственность = один интерфейс (ISP).

## 🔗 Связанные документы

- [[🔓 Open-Closed|🔓 Open-Closed]] - SRP помогает применять OCP
- [[🎯 Interface Segregation|🎯 Interface Segregation]] - Одна ответственность = один интерфейс
- [[🏗️ Dependency Inversion|🏗️ Dependency Inversion]] - Маленькие классы легче заменять
- [[🎯 SOLID|🎯 SOLID]] - Обзор всех принципов
- [[🎓 ООП|🎓 ООП]] - Основы ООП

---

*Один класс — одна ответственность. Это просто, но эффективно.*

