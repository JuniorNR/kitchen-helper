# 🔐 Auth Feature

Аутентификация и авторизация пользователей.

## 📍 Расположение

`src/features/Auth/`

## 📂 Структура

```
Auth/
├── index.ts                    # Публичный API
├── model/
│   ├── auth.api.ts           # RTK Query API
│   ├── auth.slice.ts         # Redux slice
│   ├── auth.schemas.ts       # Zod схемы валидации
│   ├── auth.types.ts        # TypeScript типы
│   └── useAuth.ts            # Хук для работы с аутентификацией
└── ui/
    ├── AuthBoundary.tsx      # Компонент защиты маршрутов
    ├── LoginForm.tsx         # Форма входа
    ├── LoginModal.tsx        # Модалка входа
    ├── SignUpForm.tsx        # Форма регистрации
    └── SignUpModal.tsx       # Модалка регистрации
```

## 📄 Компоненты

### `AuthBoundary.tsx`

Компонент для защиты маршрутов. Показывает сообщение, если пользователь не авторизован.

**Использование**:
```typescript
<AuthBoundary>
  <ProtectedContent />
</AuthBoundary>
```

---

### `LoginForm.tsx` / `LoginModal.tsx`

Форма и модальное окно для входа.

**Функциональность**:
- Валидация через Zod
- Обработка ошибок
- Уведомления об успехе/ошибке

---

### `SignUpForm.tsx` / `SignUpModal.tsx`

Форма и модальное окно для регистрации.

**Функциональность**:
- Валидация через Zod
- Проверка совпадения паролей
- Обработка ошибок

---

## 🔧 API

### `auth.api.ts`

RTK Query API с endpoints:
- `signUp` - регистрация
- `login` - вход
- `logout` - выход

**Особенности**:
- Автоматическое сохранение токена в localStorage
- Обновление состояния аутентификации
- Инвалидация кэша при входе/выходе

---

## 🗄️ Redux

### `auth.slice.ts`

Redux slice для состояния аутентификации:
- `isAuthenticated` - флаг авторизации
- `authToken` - токен авторизации

---

## ✅ Валидация

### `auth.schemas.ts`

Zod схемы для валидации форм:
- `createSignUpSchema` - схема регистрации
- `createLoginSchema` - схема входа

**Правила**:
- Email должен быть валидным
- Пароль минимум 8 символов
- Пароли должны совпадать (регистрация)

---

## 🪝 Хук

### `useAuth.ts`

Кастомный хук для работы с аутентификацией.

**Возвращает**:
- `signUpData` - функция регистрации
- `loginData` - функция входа
- `logoutData` - функция выхода
- Состояния загрузки для каждой операции

---

## 🔗 Связанные документы

- [[👤 User Entity|📦 Entities/👤 User]]
- [[🔧 Shared/lib/store|Redux Store]]

