# ⚙️ Конфигурация

Документация конфигурационных файлов проекта Kitchen Helper.

## 📂 Структура

```
configs/
├── api.config.ts           # Конфигурация API
└── layout.config.ts        # Конфигурация layout
```

## 📄 Файлы конфигурации

### [[⚙️ Конфигурация/🌐 api.config|🌐 api.config.ts]]

Конфигурация для работы с API бэкенда.

**Расположение**: `src/configs/api.config.ts`

**Экспорты**:
- `apiConfig` - объект с настройками API

**Свойства**:
- `isProd` - флаг продакшн окружения
- `APP_BACKEND_URL` - URL бэкенда для разработки
- `APP_BACKEND_URL_PROD` - URL бэкенда для продакшна

**Использование**:
```typescript
import { apiConfig } from '@/configs';

const url = apiConfig.isProd 
  ? apiConfig.APP_BACKEND_URL_PROD 
  : apiConfig.APP_BACKEND_URL;
```

**Связанные документы**:
- [[🔧 Shared/lib/configs/echo|Echo конфигурация]]

---

### [[⚙️ Конфигурация/📐 layout.config|📐 layout.config.ts]]

Конфигурация размеров layout компонентов.

**Расположение**: `src/configs/layout.config.ts`

**Экспорты**:
- `layoutConfig` - объект с размерами

**Свойства**:
- `headerHeight` - высота хедера (100px)
- `footerHeight` - высота футера (50px)
- `mainHeight` - высота основного контента (calc)
- `chatHeight` - высота чата (calc)

**Использование**:
```typescript
import { layoutConfig } from '@/configs';

<div style={{ height: layoutConfig.headerHeight }}>
  Header
</div>
```

**Связанные документы**:
- [[🧩 Widgets/📄 Header|Header Widget]]
- [[🧩 Widgets/📄 Footer|Footer Widget]]

---

## 🔧 Дополнительные конфигурации

### Next.js конфигурация

**Файл**: `next.config.ts`

Настройки для Next.js, включая:
- Модуляризация импортов HeroUI
- Оптимизация бандла

### Tailwind конфигурация

**Файл**: `tailwind.config.ts`

Настройки Tailwind CSS:
- Кастомные breakpoints (xs, sm, md, lg, xl, 2xl)
- Расширение темы

### Echo конфигурация

**Файл**: `src/shared/lib/configs/echo/echo.config.ts`

Конфигурация Laravel Echo для real-time коммуникации.

**Функции**:
- `initEcho(authToken: string)` - инициализация Echo с токеном авторизации

**Переменные окружения**:
- `NEXT_PUBLIC_APP_BACKEND_URL` - URL бэкенда
- `NEXT_PUBLIC_PUSHER_APP_KEY` - ключ Pusher
- `NEXT_PUBLIC_PUSHER_CLUSTER` - кластер Pusher (по умолчанию 'eu')

**Связанные документы**:
- [[💬 Chat|Chat Feature]]
- [[🔧 Shared/lib/providers/EchoProvider|Echo Provider]]

---

## 🔗 Связанные документы

- [[📁 Структура проекта|📁 Структура проекта]]
- [[🔧 Shared|🔧 Shared]]
- [[🌐 api.config|⚙️ Конфигурация/🌐 api.config]]
- [[📐 layout.config|⚙️ Конфигурация/📐 layout.config]]

