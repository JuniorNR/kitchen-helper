# 📐 layout.config.ts

Конфигурационный файл для размеров layout компонентов.

## 📍 Расположение

`src/configs/layout.config.ts`

## 📝 Описание

Определяет высоты основных компонентов layout приложения (header, footer, main, chat) и вычисляет динамические размеры.

## 🔧 Экспорты

### `layoutConfig`

Объект конфигурации layout со следующими свойствами:

```typescript
{
  headerHeight: string;   // Высота хедера
  footerHeight: string;   // Высота футера
  mainHeight: string;     // Высота основного контента
  chatHeight: string;     // Высота чата
}
```

## 📊 Значения

- `headerHeight`: `'100px'`
- `footerHeight`: `'50px'`
- `mainHeight`: `calc(100vh - 100px - 50px)` (вычисляется автоматически)
- `chatHeight`: `calc(100vh - 100px - 50px - 44px)` (вычисляется автоматически)

## 💡 Использование

```typescript
import { layoutConfig } from '@/configs';

// В компонентах
<div style={{ height: layoutConfig.headerHeight }}>
  Header content
</div>

// В Tailwind классах (с использованием template literals)
<div className={`h-[${layoutConfig.chatHeight}]`}>
  Chat content
</div>
```

## 🎯 Применение

Используется в:
- [[layout|Root Layout]] - для настройки основных размеров
- [[📄 Header|Header Widget]] - для установки высоты хедера
- [[📄 Footer|Footer Widget]] - для установки высоты футера
- [[FrontEnd/Kitchen-helper/docs/🎨 Features/💬 Chat|Chat Feature]] - для установки высоты чата

## 🔗 Связанные документы

- [[⚙️ Конфигурация|⚙️ Конфигурация]]
- [[layout|Root Layout]]
- [[🧩 Widgets|🧩 Widgets]]

