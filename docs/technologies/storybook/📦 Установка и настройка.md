# 📦 Установка и настройка Storybook

## 📥 Установка

### Автоматическая установка (рекомендуется)

Для проектов на Next.js с Vite используйте автоматическую установку:

```bash
# С помощью npx
npx storybook@latest init

# Или с указанием пакетного менеджера
npx storybook@latest init --package-manager pnpm
```

### Ручная установка

Если нужно установить вручную:

```bash
# Установка Storybook и необходимых пакетов
pnpm add -D storybook @storybook/nextjs-vite

# Или с npm
npm install -D storybook @storybook/nextjs-vite

# Или с yarn
yarn add -D storybook @storybook/nextjs-vite
```

### Зависимости проекта

В проекте используются следующие зависимости:

```json
{
  "devDependencies": {
    "storybook": "^10.1.5",
    "@storybook/nextjs-vite": "^10.1.5",
    "vite": "^7.2.7",
    "eslint-plugin-storybook": "^10.1.5"
  }
}
```

## 🚀 Запуск Storybook

### Режим разработки

```bash
# Запуск Storybook в режиме разработки
pnpm storybook

# Или через npm
npm run storybook

# Или через yarn
yarn storybook
```

Storybook запустится на `http://localhost:6006`

### Сборка для продакшена

```bash
# Сборка статической версии Storybook
pnpm build-storybook

# Или через npm
npm run build-storybook

# Или через yarn
yarn build-storybook
```

Собранные файлы будут в папке `storybook-static/`

## 📁 Структура файлов

После установки Storybook создает следующую структуру:

```
.storybook/
├── main.ts          # Основная конфигурация Storybook
└── preview.ts       # Настройки предпросмотра компонентов

src/stories/         # Примеры stories (можно удалить)
├── Button.stories.ts
├── Button.tsx
└── ...
```

## ⚙️ Скрипты в package.json

Добавьте следующие скрипты в `package.json`:

```json
{
  "scripts": {
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  }
}
```

## 🔧 Первоначальная настройка

### 1. Конфигурация main.ts

Файл `.storybook/main.ts` определяет:
- Какие stories загружать
- Какие аддоны использовать
- Настройки сборки

Пример конфигурации для Next.js:

```typescript
import type { StorybookConfig } from '@storybook/nextjs-vite';

const config: StorybookConfig = {
  stories: [
    "../src/**/*.stories.@(js|jsx|mjs|ts|tsx)"
  ],
  addons: [],
  framework: {
    name: "@storybook/nextjs-vite",
    options: {}
  },
  staticDirs: ["../public"]
};

export default config;
```

### 2. Конфигурация preview.ts

Файл `.storybook/preview.ts` настраивает:
- Глобальные декораторы
- Параметры по умолчанию
- Темы и стили

## ✅ Проверка установки

После установки выполните:

1. Запустите Storybook: `pnpm storybook`
2. Откройте браузер на `http://localhost:6006`
3. Убедитесь, что примеры stories отображаются корректно

## 🔗 Следующие шаги

- [[🔧 Конфигурация|🔧 Конфигурация]] - Настройка Storybook
- [[📝 Создание Stories|📝 Создание Stories]] - Создание ваших первых stories
- [[✨ Best Practices|✨ Best Practices]] - Лучшие практики работы со Storybook

## 🔗 Связанные документы

- [[🔧 Technologies|technologies/🔧 Technologies]] - Главная страница раздела Technologies
- [[📚 Storybook|📚 Storybook]] - Обзор Storybook
- [[🔧 Конфигурация|🔧 Конфигурация]] - Настройка Storybook
- [[📝 Создание Stories|📝 Создание Stories]] - Создание stories
- [[✨ Best Practices|✨ Best Practices]] - Лучшие практики
- [[🚀 Запуск и деплой|🚀 Запуск и деплой]] - Запуск и деплой Storybook

