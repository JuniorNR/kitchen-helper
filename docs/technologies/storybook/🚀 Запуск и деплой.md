# 🚀 Запуск и деплой Storybook

## 🖥️ Локальная разработка

### Запуск в режиме разработки

```bash
# С помощью pnpm
pnpm storybook

# Или через npm
npm run storybook

# Или через yarn
yarn storybook
```

Storybook запустится на `http://localhost:6006`

### Изменение порта

Если порт 6006 занят, можно указать другой:

```bash
pnpm storybook -- -p 6007
```

Или изменить в `package.json`:

```json
{
  "scripts": {
    "storybook": "storybook dev -p 6007"
  }
}
```

## 📦 Сборка для продакшена

### Сборка статической версии

```bash
# С помощью pnpm
pnpm build-storybook

# Или через npm
npm run build-storybook

# Или через yarn
yarn build-storybook
```

Собранные файлы будут в папке `storybook-static/`

### Просмотр собранной версии локально

```bash
# После сборки можно запустить локальный сервер
npx serve storybook-static
```

## 🌐 Деплой Storybook

### GitHub Pages

1. Соберите Storybook:
```bash
pnpm build-storybook
```

2. Настройте GitHub Actions (`.github/workflows/storybook.yml`):

```yaml
name: Deploy Storybook

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 9
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm build-storybook
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./storybook-static
```

3. Включите GitHub Pages в настройках репозитория

### Netlify

1. Создайте файл `netlify.toml`:

```toml
[build]
  command = "pnpm build-storybook"
  publish = "storybook-static"
```

2. Подключите репозиторий к Netlify
3. Netlify автоматически задеплоит Storybook

### Vercel

1. Создайте файл `vercel.json`:

```json
{
  "buildCommand": "pnpm build-storybook",
  "outputDirectory": "storybook-static"
}
```

2. Подключите репозиторий к Vercel
3. Vercel автоматически задеплоит Storybook

### Chromatic (рекомендуется)

Chromatic — специальный сервис для деплоя и визуального тестирования Storybook.

1. Установите Chromatic:
```bash
pnpm add -D chromatic
```

2. Запустите деплой:
```bash
npx chromatic --project-token=your-project-token
```

3. Или добавьте в `package.json`:
```json
{
  "scripts": {
    "chromatic": "chromatic --project-token=your-project-token"
  }
}
```

### Другие платформы

Storybook можно задеплоить на любую платформу, которая поддерживает статические сайты:
- **AWS S3 + CloudFront**
- **Azure Static Web Apps**
- **Firebase Hosting**
- **Surge.sh**
- **GitLab Pages**

## 🔧 Настройка для деплоя

### Базовый URL

Если Storybook деплоится в подпапку, настройте базовый URL в `main.ts`:

```typescript
const config: StorybookConfig = {
  // ... другие настройки
  base: '/storybook/', // Если деплоится в /storybook/
};
```

### Окружения

Можно использовать разные конфигурации для разных окружений:

```typescript
const config: StorybookConfig = {
  // ... настройки
  env: (config) => ({
    ...config,
    API_URL: process.env.API_URL || 'http://localhost:3000',
  }),
};
```

## 📝 CI/CD интеграция

### GitHub Actions пример

```yaml
name: Storybook CI

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm build-storybook
      - name: Upload Storybook
        uses: actions/upload-artifact@v3
        with:
          name: storybook
          path: storybook-static
```

## 🔗 Связанные документы

- [[🔧 Technologies|technologies/🔧 Technologies]] - Главная страница раздела Technologies
- [[📚 Storybook|📚 Storybook]] - Обзор Storybook
- [[📦 Установка и настройка|📦 Установка и настройка]] - Установка Storybook
- [[🔧 Конфигурация|🔧 Конфигурация]] - Настройка Storybook
- [[📝 Создание Stories|📝 Создание Stories]] - Создание stories
- [[✨ Best Practices|✨ Best Practices]] - Лучшие практики


