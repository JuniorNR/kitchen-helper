# 🔧 Конфигурация Storybook

## 📁 Основные файлы конфигурации

### `.storybook/main.ts`

Главный конфигурационный файл Storybook определяет:
- Какие stories загружать
- Какие аддоны использовать
- Настройки сборки
- Интеграции с фреймворками

#### Пример конфигурации для Next.js

```typescript
import type { StorybookConfig } from '@storybook/nextjs-vite';

const config: StorybookConfig = {
  // Пути к stories файлам
  stories: [
    "../src/**/*.stories.@(js|jsx|mjs|ts|tsx)"
  ],
  
  // Аддоны
  addons: [
    "@storybook/addon-essentials",
    "@storybook/addon-a11y",
    "@storybook/addon-docs"
  ],
  
  // Фреймворк
  framework: {
    name: "@storybook/nextjs-vite",
    options: {}
  },
  
  // Статические файлы
  staticDirs: ["../public"],
  
  // Настройки TypeScript
  typescript: {
    check: false,
    reactDocgen: "react-docgen-typescript",
    reactDocgenTypescriptOptions: {
      shouldExtractLiteralValuesFromEnum: true,
      propFilter: (prop) => (prop.parent ? !/node_modules/.test(prop.parent.fileName) : true),
    },
  },
};

export default config;
```

### `.storybook/preview.ts`

Настройки предпросмотра компонентов:
- Глобальные декораторы
- Параметры по умолчанию
- Темы и глобальные стили
- Глобальные типы

#### Пример конфигурации preview

```typescript
import type { Preview } from '@storybook/react';
import '../src/app/globals.css'; // Импорт глобальных стилей

const preview: Preview = {
  parameters: {
    // Настройки для всех stories
    actions: { argTypesRegex: "^on[A-Z].*" },
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/i,
      },
    },
    // Настройки документации
    docs: {
      toc: true,
    },
    // Настройки доступности
    a11y: {
      config: {
        rules: [
          {
            id: 'color-contrast',
            enabled: true,
          },
        ],
      },
    },
  },
  // Глобальные декораторы
  decorators: [
    (Story) => (
      <div style={{ padding: '20px' }}>
        <Story />
      </div>
    ),
  ],
};

export default preview;
```

## 🔌 Аддоны

Storybook поддерживает множество аддонов для расширения функциональности.

### Основные аддоны

#### @storybook/addon-essentials
Включает основные аддоны:
- **Actions** - логирование событий
- **Controls** - интерактивное изменение props
- **Docs** - автоматическая документация
- **Viewport** - тестирование разных размеров экрана
- **Backgrounds** - изменение фона
- **Measure** - измерение элементов
- **Outline** - контуры элементов

Установка:
```bash
pnpm add -D @storybook/addon-essentials
```

#### @storybook/addon-a11y
Тестирование доступности компонентов.

Установка:
```bash
pnpm add -D @storybook/addon-a11y
```

Использование в `main.ts`:
```typescript
addons: [
  "@storybook/addon-a11y"
]
```

#### @storybook/addon-docs
Расширенная документация компонентов.

Установка:
```bash
pnpm add -D @storybook/addon-docs
```

### Другие полезные аддоны

- `@storybook/addon-viewport` - тестирование разных размеров экрана
- `@storybook/addon-backgrounds` - изменение фона
- `@storybook/addon-measure` - измерение элементов
- `@storybook/addon-outline` - контуры элементов
- `@storybook/addon-interactions` - тестирование взаимодействий
- `@storybook/addon-coverage` - покрытие кода тестами

## 🎨 Настройка тем

### Темная/светлая тема

В `preview.ts`:

```typescript
import { themes } from '@storybook/theming';

const preview: Preview = {
  parameters: {
    backgrounds: {
      default: 'light',
      values: [
        {
          name: 'light',
          value: '#ffffff',
        },
        {
          name: 'dark',
          value: '#333333',
        },
      ],
    },
  },
};
```

## 🔗 Связанные документы

- [[🔧 Technologies|technologies/🔧 Technologies]] - Главная страница раздела Technologies
- [[📚 Storybook|📚 Storybook]] - Обзор Storybook
- [[📦 Установка и настройка|📦 Установка и настройка]] - Установка Storybook
- [[🎭 Декораторы|🎭 Декораторы]] - Добавление глобальных декораторов в preview
- [[📝 Создание Stories|📝 Создание Stories]] - Создание stories
- [[✨ Best Practices|✨ Best Practices]] - Лучшие практики
- [[🚀 Запуск и деплой|🚀 Запуск и деплой]] - Запуск и деплой Storybook

