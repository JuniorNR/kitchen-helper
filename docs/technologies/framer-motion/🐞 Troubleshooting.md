# 🐞 Troubleshooting

## 😵 Гидрация мигает или крашится
- Причина: разные `initial` на сервере и клиенте.  
  **Фикс:** `initial={false}` у `AnimatePresence` или задайте `initial` равным `animate`.
- Убедитесь, что компонент — клиентский (`'use client'`) и не использует браузерные API до маунта.

## 🙅‍♂️ `exit` не срабатывает
- Нет `AnimatePresence` или он не оборачивает элемент, который удаляется.
- Ключи в списке нестабильны (`index`/рандом). Используйте реальные `id`.
- Проп `mode` не подходит: попробуйте `mode="popLayout"` или `mode="wait"` для роутов.

## 🫨 Дёргается layout
- Лишний `layout` на элементах, которые не должны пересчитываться — снимите.
- Нет базовых размеров: задайте min-height/width через класс, чтобы избежать скачков.
- Позиционирование: убедитесь, что родитель `position: relative`, если применяете `layout`.

## 🧭 Жесты конфликтуют со скроллом
- При `drag` ставьте `drag="x"`/`"y"` и `dragConstraints`.  
- Если скролл блокируется — ограничьте `whileTap` по масштабу (не < 0.95) или снимите `drag` на мобильных.

## 🧪 Тесты ломаются
- Для Jest/RTL мокните `framer-motion` или используйте `jest.mock('framer-motion', () => require('framer-motion/dist/es/components/AnimatePresence/AnimatePresence').mock);` либо обёртку, которая возвращает статичные элементы.
- В Storybook добавляйте `animate={false}` или knob для отключения анимации, чтобы избежать flaky снимков.

## 🔗 Навигация
- Назад: [[✅ Чеклист|framer-motion/✅ Чеклист]]
- Обзор: [[🎞️ Framer Motion|framer-motion/🎞️ Framer Motion]]
- Связанные: [[🎬 Паттерны и примеры|framer-motion/🎬 Паттерны и примеры]], [[🧪 Jest + React Testing Library|technologies/jest-rtl/🧪 Jest + React Testing Library]]
