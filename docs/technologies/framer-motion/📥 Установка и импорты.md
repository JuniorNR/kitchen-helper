# 📥 Установка и импорты

## 📦 Зависимости
- В проекте уже используется `framer-motion@12` (см. `package.json`).  
- При необходимости установки/обновления:  
  - `pnpm add framer-motion`

Дополнительных тайпингов не нужно: библиотека типизирована из коробки.

## 🏷️ Импорты (React 19)
- Базовый набор:  
  ```tsx
  import { motion, AnimatePresence } from 'framer-motion';
  ```
- Экономия бандла через `LazyMotion` (по умолчанию `domAnimation`):  
  ```tsx
  import { LazyMotion, domAnimation, m } from 'framer-motion';
  ```
- Для сенсоров и жестов:  
  ```tsx
  import { useMotionValue, useSpring, useInView } from 'framer-motion';
  ```

## 🧩 Где ставить `use client`
- Любой компонент, использующий `motion`/`AnimatePresence`/хуки framer-motion, должен начинаться с `'use client'` (иначе Next.js не позволит взаимодействия).
- Старайтесь выносить анимации в клиентские "обёртки", чтобы не размечать весь экран как client component.

## 🔗 Навигация
- Следующий: [[⚙️ Конфигурация под Next.js|framer-motion/⚙️ Конфигурация под Next.js]]
- Назад: [[🎞️ Framer Motion|framer-motion/🎞️ Framer Motion]]
- Связанные: [[🧭 Базовые концепции|framer-motion/🧭 Базовые концепции]], [[🎬 Паттерны и примеры|framer-motion/🎬 Паттерны и примеры]]
