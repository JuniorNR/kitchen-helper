# 🧭 Базовые концепции

## 🔤 Основные сущности
- **`motion.*`** — обёртки над HTML/SVG для анимаций.
- **`initial / animate / exit`** — стартовое, целевое и выходное состояние.
- **`transition`** — настройки перехода (duration, ease, type, stiffness, damping).
- **`variants`** — именованные наборы состояний для переиспользования.
- **`AnimatePresence`** — включает `exit` анимации при удалении из React-дерева.
- **`layout` / `layoutId`** — анимируют изменения размеров/позиции, делают shared layout transitions.
- **Жесты**: `whileHover`, `whileTap`, `drag`, `whileInView`.

## 🧱 Variants
Используйте variants, чтобы описывать состояния один раз и переиспользовать.
```tsx
const cardVariants = {
  hidden: { opacity: 0, y: 12 },
  show:   { opacity: 1, y: 0 },
  exit:   { opacity: 0, y: -8 },
};

<motion.article
  variants={cardVariants}
  initial="hidden"
  animate="show"
  exit="exit"
  transition={{ duration: 0.18, ease: 'easeOut' }}
/>
```

## 🌀 Жесты
```tsx
<motion.button
  whileHover={{ scale: 1.02 }}
  whileTap={{ scale: 0.98 }}
  transition={{ type: 'spring', stiffness: 280, damping: 18 }}
>
  Кнопка
</motion.button>
```

## 🗂️ AnimatePresence
```tsx
<AnimatePresence mode="popLayout" initial={false}>
  {items.map(item => (
    <motion.li
      key={item.id}
      initial={{ opacity: 0, height: 0 }}
      animate={{ opacity: 1, height: 'auto' }}
      exit={{ opacity: 0, height: 0 }}
    >
      {item.title}
    </motion.li>
  ))}
  </AnimatePresence>
```
Важно: у элементов должны быть стабильные `key`, иначе `exit` не сработает.

## 📐 Layout transitions
```tsx
<motion.img
  src={src}
  alt={alt}
  layoutId={`image-${id}`} // общий id между списком и модалкой
/>
```
Одинаковый `layoutId` на двух состояниях позволяет сделать плавный морфинг (например, превью → модалка).

## 🔗 Навигация
- Следующий: [[🎬 Паттерны и примеры|framer-motion/🎬 Паттерны и примеры]]
- Назад: [[⚙️ Конфигурация под Next.js|framer-motion/⚙️ Конфигурация под Next.js]]
- Связанные: [[💡 Best Practices|framer-motion/💡 Best Practices]], [[✅ Чеклист|framer-motion/✅ Чеклист]]
