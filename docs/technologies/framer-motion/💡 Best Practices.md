# 💡 Best Practices

## ⚖️ UX и доступность
- Учитывайте `prefers-reduced-motion`: заменяйте сложные переходы на простое появление.
- Длительность для UI-микроанимаций: 120–240 мс, ease: `easeOut` или spring со слабым овердшутом.
- Не блокируйте ввод во время анимации; транзишены должны быть отменяемыми.

## 🧩 Архитектура
- Держите анимацию рядом с компонентом: локальный `AnimatePresence` для списков, отдельный для модалок.
- Используйте `variants` и `transition` как константы вне компонента, чтобы не пересоздавать объекты.
- Для богатых страниц применяйте `LazyMotion` с `domAnimation`/`domMax` и оборачивайте всё дерево, чтобы выгружать фичи один раз.

## 🏎️ Производительность
- Анимируйте `transform` и `opacity`, избегайте `width/height/left/top`, когда возможно.
- Для layout-анимаций ставьте `layout` только на элементы, где это нужно; лишние `layout` вызывают перерасчёт.
- Следите за ключами в списках: стабильный `key` = корректный `exit`.
- Не злоупотребляйте `whileInView`: используйте `viewport={{ once: true }}` и разумный `amount`.

## 🛠️ Интеграция с Tailwind/SCSS
- Сначала задайте статичное состояние стилями, затем добавляйте `motion`-пропсы — так меньше дрожаний при гидрации.
- Анимация цвета/тени — через `boxShadow`/`filter` либо предварительно определённые классы, чтобы не менять десятки CSS-свойств.

## 🔗 Навигация
- Следующий: [[✅ Чеклист|framer-motion/✅ Чеклист]]
- Назад: [[🎬 Паттерны и примеры|framer-motion/🎬 Паттерны и примеры]]
- Связанные: [[🐞 Troubleshooting|framer-motion/🐞 Troubleshooting]], [[🎞️ Framer Motion|framer-motion/🎞️ Framer Motion]]
