# 🧪 Примеры тестов

## Компонент (React Testing Library)
```typescript
// src/features/ThemeSwitcher/ui/ThemeSwitcher.test.tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { ThemeSwitcher } from "./ThemeSwitcher";

describe("ThemeSwitcher", () => {
  it("переключает тему", async () => {
    render(<ThemeSwitcher />);
    const button = screen.getByRole("button");
    await userEvent.click(button);
    expect(button).toHaveAttribute("aria-pressed", "true");
  });
});
```

## Функция/хук (Jest)
```typescript
// src/shared/lib/helpers/formatPrice.test.ts
import { formatPrice } from "./formatPrice";

describe("formatPrice", () => {
  it("форматирует цену с валютой", () => {
    expect(formatPrice(1234.5, "₽")).toBe("1 234,50 ₽");
  });
});
```

Как запускать примеры:
- Локально: `pnpm test src/features/ThemeSwitcher/ui/ThemeSwitcher.test.tsx`
- В режиме наблюдения: `pnpm test:watch`
- С покрытием: `pnpm test:coverage`

Что проверить после запуска:
- Тесты проходят без ошибок.
- Вывод показывает, что используется среда `jsdom`.
- Нет жалоб на отсутствующие моки (`fetch`, `next/navigation`, статика).

## ↔ Навигация
- Назад: [[📁 Структура и моки|jest-rtl/📁 Структура и моки]]
- Далее: [[✅ Чеклист|jest-rtl/✅ Чеклист]]

