# 📁 Структура и моки

## Рекомендуемая структура
- Тесты рядом с кодом: `Component.test.tsx`, `helper.test.ts`.
- Общий `setupTests.ts` в корне репозитория.
- Папка `__mocks__/` для статики и кастомных моков.

Почему так:
- Тест рядом с компонентом/функцией облегчает навигацию и рефакторинг.
- Единый `setupTests.ts` подключает матчеры и полифилы один раз.
- Моки сложных зависимостей (Next, статика) лежат в `__mocks__`, чтобы переиспользовать.

## Мок статики
`__mocks__/fileMock.js`
```javascript
module.exports = "test-file-stub";
```

## Моки Next.js
App Router (`next/navigation`):
```typescript
// mock-next-navigation.ts
jest.mock("next/navigation", () => ({
  useRouter: () => ({
    push: jest.fn(),
    replace: jest.fn(),
    refresh: jest.fn(),
    back: jest.fn(),
    forward: jest.fn(),
    prefetch: jest.fn(),
    pathname: "/",
    query: {},
    params: {}
  })
}));
```
Pages Router (`next/router`) мокается аналогично. При необходимости добавляйте моки `next/image` или других модулей.

## ↔ Навигация
- Назад: [[⚙️ Конфигурация|jest-rtl/⚙️ Конфигурация]]
- Далее: [[🧪 Примеры тестов|jest-rtl/🧪 Примеры тестов]]

