# 📥 Установка (Jest + RTL)

## 🎯 Цель
Подготовить окружение для юнит и компонентных тестов в Kitchen Helper (Next.js 15, React 19, TS, pnpm, Storybook).

## 📦 Основные dev-зависимости
```bash
pnpm add -D jest @types/jest jest-environment-jsdom \
  @testing-library/react @testing-library/jest-dom @testing-library/user-event \
  identity-obj-proxy whatwg-fetch
```

### Опционально
- `pnpm add -D msw` — моки API (удобно шарить со Storybook).
- Если нужны снапшоты картинок/статик — используйте файл-мок (см. структуру).

## 🪜 Шаги установки
1) Выполните команду выше в корне проекта (`pnpm` обязателен, т.к. задан в `packageManager`).  
2) Проверьте, что в `package.json` появились зависимости (или зафиксируйте лок-файл).  
3) Если планируете моки API — сразу добавьте `msw`, чтобы не править конфиг дважды.  
4) Перейдите к [[⚙️ Конфигурация|jest-rtl/⚙️ Конфигурация]] и настройте файлы.

## ↔ Навигация
- Назад: [[🧪 Jest + React Testing Library|jest-rtl/🧪 Jest + React Testing Library]]
- Далее: [[⚙️ Конфигурация|jest-rtl/⚙️ Конфигурация]]

