# ✅ Чеклист

1) Установить зависимости (`pnpm add -D ...` из раздела Установка).  
2) Создать `jest.config.ts` и `setupTests.ts`, добавить `__mocks__/fileMock.js`.  
3) Прописать скрипты `test`, `test:watch`, `test:coverage` в `package.json`.  
4) Написать первый юнит (чистая функция) и первый компонентный тест (RTL).  
5) Запустить `pnpm test` и убедиться, что среда `jsdom`, моки работают.  
6) (Опционально) Подключить MSW и вынести хендлеры, переиспользуемые в Storybook.

## ↔ Навигация
- Назад: [[🧪 Примеры тестов|jest-rtl/🧪 Примеры тестов]]
- Далее: [[💡 Best Practices|jest-rtl/💡 Best Practices]]

