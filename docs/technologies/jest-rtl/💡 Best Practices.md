# 💡 Best Practices

- Используйте `screen.findBy*` для асинхронных состояний, `getBy*` для синхронных.
- Опираться на доступность (role/name), а не на `data-testid`.
- Минимум снапшотов — проверяйте текст, роли и важные атрибуты.
- Провайдеры в тестах должны быть максимально близки к реальному дереву (сторы/темы/i18n).
- Моки сети через MSW — один источник правды для тестов и Storybook.

Дополнительно под стек:
- Для Next app router держите моки `next/navigation` единообразно и переиспользуйте.  
- Если тестируете формы — используйте `userEvent` вместо `fireEvent` для ближнего к пользователю поведения.  
- Для сложных контекстов (i18n, тема, store) создайте тестовый рендер-хелпер с провайдерами.

## ↔ Навигация
- Назад: [[✅ Чеклист|jest-rtl/✅ Чеклист]]
- Далее: [[🐞 Troubleshooting|jest-rtl/🐞 Troubleshooting]]

