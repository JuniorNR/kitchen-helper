# 📅 parseDate.ts

Функция парсинга даты из строки.

## 📍 Расположение

`src/shared/lib/helpers/parseDate/parseDate.ts`

## 📝 Описание

Парсит строку с датой в объект Date с учетом локали.

## 🔧 Сигнатура

```typescript
export const parseDate = (
  value: string,
  locale: string
): Date | null
```

## 💡 Использование

```typescript
import { parseDate } from '@/shared/lib/helpers';

// Русский формат: DD.MM.YYYY
const date = parseDate('25.12.2024', 'ru');
// Результат: Date object или null

if (date) {
  console.log(date.getFullYear()); // 2024
  console.log(date.getMonth()); // 11 (декабрь, 0-indexed)
  console.log(date.getDate()); // 25
}
```

## 📋 Поддерживаемые форматы

### Русский (ru)
- Формат: `DD.MM.YYYY`
- Пример: `25.12.2024`

## ✅ Валидация

Функция проверяет:
- Корректность формата
- Валидность даты (например, не допускает 32.01.2024)
- Соответствие разобранных значений реальной дате

## ⚠️ Особенности

- Возвращает `null` если формат неверный или дата невалидна
- Поддерживает только русский формат (ru)
- Для других локалей возвращает `null`

## 🔗 Связанные документы

- [[🔧 Shared/lib/helpers|Вспомогательные функции]]
- [[🔧 Shared/lib/helpers/serializeDate|serializeDate]]

