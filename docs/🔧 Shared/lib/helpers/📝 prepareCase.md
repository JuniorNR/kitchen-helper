# 📝 prepareCase.ts

Функция преобразования регистра строк (camelCase ↔ snake_case).

## 📍 Расположение

`src/shared/lib/helpers/prepareCase/prepareCase.ts`

## 📝 Описание

Преобразует строку между форматами camelCase и snake_case с автоматическим определением формата.

## 🔧 Сигнатура

```typescript
export const prepareCase = (
  value: string,
  target?: 'camel' | 'snake',
): string
```

## 💡 Использование

### Автоматическое определение

```typescript
import { prepareCase } from '@/shared/lib/helpers';

// Из snake_case в camelCase
const result1 = prepareCase('user_name');
// Результат: "userName"

// Из camelCase в snake_case
const result2 = prepareCase('userName');
// Результат: "user_name"
```

### Явное указание целевого формата

```typescript
// Принудительно в snake_case
const result = prepareCase('userName', 'snake');
// Результат: "user_name"

// Принудительно в camelCase
const result2 = prepareCase('user_name', 'camel');
// Результат: "userName"
```

### Сложные случаи

```typescript
prepareCase('API_KEY'); // "api_key"
prepareCase('api_key'); // "apiKey"
prepareCase('userName'); // "user_name"
prepareCase('XMLHttpRequest'); // "xml_http_request"
```

## 🔄 Алгоритм

### В snake_case
1. Разделяет по границе строчных и заглавных букв
2. Объединяет через `_`
3. Приводит к нижнему регистру

### В camelCase
1. Разделяет по `_`
2. Первая часть остается в нижнем регистре
3. Остальные части: первая буква в верхнем регистре
4. Объединяет без разделителей

## ⚠️ Особенности

- Пустые строки возвращаются как есть
- Если формат не определен, пытается угадать по наличию `_`
- Обрабатывает множественные подчеркивания
- Удаляет подчеркивания в начале и конце

## 🔗 Связанные документы

- [[🔧 Shared/lib/helpers|Вспомогательные функции]]
- [[🔧 Shared/lib/helpers/📦 dto|dto]]

