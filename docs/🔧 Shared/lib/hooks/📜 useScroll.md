# 📜 useScroll.tsx

Хук для отслеживания позиции скролла.

## 📍 Расположение

`src/shared/lib/hooks/useScroll/useScroll.tsx`

## 📝 Описание

Отслеживает позицию скролла по оси X или Y для указанного элемента или окна.

## 🔧 Сигнатура

```typescript
export const useScroll = (
  direction: 'x' | 'y',
  target?: Element | string | null | undefined
): number
```

## 💡 Использование

### Отслеживание скролла окна

```typescript
import { useScroll } from '@/shared/lib/hooks';

const MyComponent = () => {
  // Отслеживание вертикального скролла окна
  const scrollY = useScroll('y');
  
  // Отслеживание горизонтального скролла окна
  const scrollX = useScroll('x');

  return (
    <div>
      <p>Вертикальный скролл: {scrollY}px</p>
      <p>Горизонтальный скролл: {scrollX}px</p>
    </div>
  );
};
```

### Отслеживание скролла элемента по селектору

```typescript
const scrollY = useScroll('y', '#my-scrollable-div');
```

### Отслеживание скролла элемента по ссылке

```typescript
const MyComponent = () => {
  const divRef = useRef<HTMLDivElement>(null);
  const scrollY = useScroll('y', divRef.current);

  return (
    <div ref={divRef} style={{ overflow: 'auto', height: '400px' }}>
      {/* Контент */}
    </div>
  );
};
```

### Использование в Header

```typescript
// Пример из Header widget
const scrollY = useScroll('y');
const isScrolled = scrollY > 4;

// Применение эффекта при скролле
<div className={isScrolled ? 'backdrop-blur-md shadow-lg' : ''}>
  Header content
</div>
```

## 📊 Параметры

- `direction`: `'x'` | `'y'` - направление скролла
- `target`: `Element | string | null | undefined` - целевой элемент:
  - `undefined` или не указан - отслеживает `window`
  - `string` - CSS селектор
  - `Element` - DOM элемент

## 🔄 Возвращаемое значение

Число (number) - текущая позиция скролла в пикселях.

## ⚡ Оптимизация

- Использует `passive: true` для слушателя событий
- Автоматически очищает слушатель при размонтировании
- Обновляется только при изменении `direction` или `target`

## 🔗 Связанные документы

- [[🔧 Shared/lib/hooks|Кастомные хуки]]
- [[📄 Header|Header Widget]]

