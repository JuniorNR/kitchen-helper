# 📡 EchoProvider.tsx

Провайдер для Laravel Echo (real-time коммуникация).

## 📍 Расположение

`src/shared/lib/providers/EchoProvider/ui/EchoProvider.tsx`

## 📝 Описание

Инициализирует Laravel Echo для real-time коммуникации с бэкендом через WebSocket (Pusher).

## 🔧 Использование

```typescript
import { EchoProvider } from '@/shared/lib/providers';

// В AppProviders
<EchoProvider>
  {children}
</EchoProvider>
```

## 🔄 Логика работы

1. Получает токен авторизации из Redux store (`auth.authToken`)
2. Если токена нет в store, пытается получить из localStorage
3. Инициализирует Echo с токеном через `initEcho()`
4. Обрабатывает ошибки если токен отсутствует

## 🔐 Авторизация

Провайдер автоматически использует токен из:
1. Redux store (`state.auth.authToken`)
2. localStorage (`auth_token`) - если токена нет в store

## ⚠️ Особенности

- Инициализируется при каждом рендере (если токен изменился)
- Выводит ошибку в консоль если токен отсутствует при аутентификации
- Используется для real-time обновлений (чаты, уведомления)

## 🔗 Связанные документы

- [[🔧 Shared/lib/providers|React провайдеры]]
- [[🔧 Shared/lib/configs/echo|Echo конфигурация]]
- [[💬 Chat Feature|🎨 Features/💬 Chat]]

