# 🌐 I18nProvider.tsx

Провайдер для интернационализации (i18next).

## 📍 Расположение

`src/shared/lib/providers/I18nProvider/ui/I18nProvider.tsx`

## 📝 Описание

Инициализирует и настраивает i18next для поддержки нескольких языков в приложении.

## 🔧 Использование

```typescript
import { I18nProvider } from '@/shared/lib/providers';

// В AppProviders
<I18nProvider>
  {children}
</I18nProvider>
```

## 🌍 Поддерживаемые языки

- **en** - Английский
- **ru** - Русский

## 📚 Namespaces (пространства имен)

- `common` - общие переводы
- `fields` - поля форм
- `validation` - сообщения валидации
- `units` - единицы измерения
- `ingredients` - ингредиенты
- `alerts` - уведомления
- `recipes` - рецепты
- `chats` - чаты
- `markets` - магазины

## 🔄 Определение языка

Язык определяется автоматически в следующем порядке:
1. Query параметр (`?lng=ru`)
2. localStorage
3. Навигатор браузера
4. Cookie

## 💡 Использование в компонентах

```typescript
import { useTranslation } from 'react-i18next';

const MyComponent = () => {
  const { t } = useTranslation('common');
  
  return <h1>{t('app_name')}</h1>;
};
```

### Использование разных namespaces

```typescript
const { t: tCommon } = useTranslation('common');
const { t: tIngredients } = useTranslation('ingredients');

<h1>{tCommon('app_name')}</h1>
<p>{tIngredients('title')}</p>
```

## ⚙️ Настройки

- **Fallback язык**: `en`
- **Поддерживаемые языки**: `['en', 'ru']`
- **Namespace по умолчанию**: `common`
- **Кэширование**: localStorage
- **Интерполяция**: отключена экранирование значений

## 🔗 Связанные документы

- [[🔧 Shared/lib/providers|React провайдеры]]
- [[🌐 LanguageSwitcher Feature|🎨 Features/🌐 LanguageSwitcher]]
- [[🔧 Shared/locales|Переводы]]

