# 🖼️ ImagesPick.tsx

Компонент выбора и предпросмотра изображений.

## 📍 Расположение

`src/shared/ui/ImagesPick/ui/ImagesPick.tsx`

## 📝 Описание

Компонент для выбора нескольких изображений с предпросмотром и возможностью удаления.

## 🔧 Props

```typescript
interface ImagesPickProps {
  maxImages?: number; // По умолчанию 5
  errorMessage?: string;
  onChange?: (event: ChangeEvent<HTMLInputElement>) => void;
  // ... другие props от Input
}
```

## 💡 Использование

```typescript
import { ImagesPick } from '@/shared/ui';

const [images, setImages] = useState<File[]>([]);

<ImagesPick
  maxImages={5}
  onChange={(e) => {
    const files = Array.from(e.target.files || []);
    setImages(files);
  }}
/>
```

## 🎨 Особенности

- Предпросмотр выбранных изображений
- Ограничение количества изображений
- Удаление изображений из списка
- Автоматическая очистка URL объектов при размонтировании
- Фильтрация только изображений
- Адаптивный дизайн

## ⚠️ Особенности реализации

- Использует `URL.createObjectURL` для предпросмотра
- Автоматически освобождает память при размонтировании
- Скрытый input для выбора файлов

## 🔗 Связанные документы

- [[🔧 Shared/ui|UI компоненты]]
- [[🔧 Shared/ui/🗑️ DeleteButton|DeleteButton]]

