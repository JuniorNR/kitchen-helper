# 📏 UnitInput.tsx

Компонент ввода количества с выбором единицы измерения.

## 📍 Расположение

`src/shared/ui/UnitInput/ui/UnitInput.tsx`

## 📝 Описание

Комбинированный компонент для ввода количества и выбора единицы измерения (граммы, килограммы, литры и т.д.).

## 🔧 Props

```typescript
interface UnitInputProps {
  value: number;
  onUnitChange: (value: number) => void;
  unit: string;
  onUnitUnitChange: (unit: string) => void;
}
```

## 💡 Использование

```typescript
import { UnitInput } from '@/shared/ui';

const [amount, setAmount] = useState(100);
const [unit, setUnit] = useState('gr');

<UnitInput
  value={amount}
  onUnitChange={setAmount}
  unit={unit}
  onUnitUnitChange={setUnit}
/>
```

## 🎨 Особенности

- Группировка единиц измерения по категориям
- Минимальное значение: 1
- Скрытые стрелки увеличения/уменьшения
- Адаптивная ширина (60% для количества, 40% для единицы)
- Локализация единиц измерения

## 🔗 Связанные документы

- [[🔧 Shared/ui|UI компоненты]]
- [[🔧 Shared/lib/constants|Константы]]

