import "@testing-library/jest-dom";
// import "whatwg-fetch";

// Раскомментируйте при ошибках TextEncoder/TextDecoder в jsdom:
// import { TextEncoder, TextDecoder } from "node:util";
// Object.assign(global, { TextEncoder, TextDecoder });